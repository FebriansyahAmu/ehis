import type { Metadata } from "next";
import SdkiPage from "@/components/master/sdki/SdkiPage";

export const metadata: Metadata = { title: "SDKI / SIKI / SLKI — Master" };

export default function Page() {
  return <SdkiPage />;
}
