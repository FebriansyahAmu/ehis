# EHIS — Project Context & Active Work

> **Read this first every new session.** Lean overview — state of the project + active work only.
> **Before switching tasks:** (1) check off completed items, (2) move them to [.claude/DONE.md](.claude/DONE.md), (3) add new findings to [TECH_DEBT.md](TECH_DEBT.md).

## 🧭 Workflow Docs

| File | Purpose |
|---|---|
| [CLAUDE.md](CLAUDE.md) | **You are here.** Current state · active work · key data contracts. |
| [TODO.md](TODO.md) | Master phase roadmap — Phase 0–3 frontend ✅ 100% (30/30). |
| [TODO-BILLING.md](TODO-BILLING.md) | **Billing Kasir roadmap** — Fase BL0–BL9 (35 task, ~3 minggu). Core operasional ✅ (BL1+BL2+BL3+BL8 100% · BL6 ~80%). |
| [TODO-BILLING-BACKEND.md](TODO-BILLING-BACKEND.md) | **Billing Backend roadmap** — 📋 Planned (2026-07-13). Ganti `billingStore`/`chargeIngest` client-side dengan **schema `billing.*` (Invoice/InvoiceItem/Payment) + ingest SERVER-SIDE dalam tx aksi klinis**. Fase BB0–BB9 (±9–12 hari). Inti: **wiring matrix per sumber** (Tindakan/Lab/Rad/Resep/BMHP/Akomodasi/Administrasi — hook Service eksisting + snapshot harga beku + `sourceRef` idempoten + Void bukan delete) · prinsip B1–B10 (harga=snapshot order, total=proyeksi, TITIPAN tagih `kelasHak`, primitif tunggal `postCharges`) · interaksi lifecycle complete/reopen/cancel · gap G-1..G-7 (tarif kamar belum ada master, biaya admin, jasa dokter, reopen-vs-pembayaran). Prasyarat E-Klaim nyata. |
| [TODO-EKLAIM.md](TODO-EKLAIM.md) | **E-Klaim BPJS/Asuransi roadmap** — Fase EK0–EK9 (38 task, ~3.5-4.5 minggu). Pisah dari billing per scope-split 2026-05-24. **Pivot 2026-05-26: iDRG primary** (resmi 1 Okt 2025 Kemenkes), INA-CBG = legacy adapter Phase later. |
| [TODO-BPJS.md](TODO-BPJS.md) | **BPJS Integration Hub roadmap** — Fase BP0–BP8 (44 section). **✅ 100% SELESAI** (2026-05-30) — V-Claim 5 sub-menu + Aplicares 3 sub-menu + Audit Trail + 3 Print Template + Workflow Docs. **(FE/mock — web service NYATA = [TODO-BPJS-WS.md](TODO-BPJS-WS.md))** |
| [TODO-BPJS-WS.md](TODO-BPJS-WS.md) | **BPJS Web Service (backend NYATA) roadmap** — 📋 Planned (2026-06-26). Ganti mock `/ehis-bpjs` dengan **connector server-side**. Fase BWS0–BWS9. **Keputusan:** mulai **BWS0 Connector Spine** (signing+AES decrypt+LZ-String+HTTP client+audit DB+rate-limit), target **Sandbox** (`apijkn-dev`). Prinsip: **server-only secrets · FE→BFF `/api/v1/bpjs/*`→BPJS · pipeline decode tunggal · referensi di-sync (buka gap `skdp.kodeDPJP`) · `BPJS_MODE=mock\|sandbox\|prod`**. Aturan wajib: [docs/BPJS-WS-RULES.md](docs/BPJS-WS-RULES.md). |
| [docs/BPJS-WS-RULES.md](docs/BPJS-WS-RULES.md) | **Aturan kanonik menulis panggilan BPJS** — R1–R14: secrets server-only · FE→BFF→BPJS · layered Route→Service→Connector · panggilan eksternal di luar DB tx (outbox) · pipeline decode tunggal (AES-256-CBC→LZ-String) · signature/timestamp · idempotency write (anti SEP ganda) · audit DB 5-thn (PII di-hash) · retry/circuit-breaker · rate-limit per cons-id · referensi sync · sandbox-first. **Baca sebelum integrasi BPJS apa pun.** |
| [TODO-CLINICAL.md](TODO-CLINICAL.md) | **Rekam Medis Klinis (backend) roadmap** — 19 tab IGD → ~9 domain klinis lintas-unit (schema Postgres baru `medicalrecord`, append-only). **Domain 1 Triase: Fase A backend ✅** (2026-06-07) — `medicalrecord.Triase` + `/api/v1/kunjungan/:id/triase` GET/POST layered, sinkron `kunjungan.triaseLevel`. Sisa: Fase B/C wiring UI + domain berikutnya (Observation→CPPT→Condition→Procedure→…). **TindakanMedis ✅ (2026-06-12)** — `medicalrecord.TindakanMedis` + `/kunjungan/:id/tindakan` (CRUD layered, gate `clinical.tindakan` full CRUD Admin/Dokter/Perawat, snapshot biaya), tab Tindakan IGD persist saat kunjunganId UUID. **Keperawatan ✅ (2026-06-14, Domain 9 A+B+C)** — `medicalrecord.AsuhanKeperawatan` + `/kunjungan/:id/asuhan-keperawatan` (CRUD + verify co-sign; gate **`clinical.keperawatan`** resource baru, Perawat penulis utama); **evaluasi shift = tabel anak `medicalrecord.AsuhanEvaluasi`** append-only + endpoint `/asuhan-keperawatan/:itemId/evaluasi` (GET/POST), form DateTimePicker+Select+perawat sesi (Fase C); template asuhan dari DB `master.sdki` via `/master/sdki-template` (gate klinis); tab Keperawatan IGD wired (tanggal=DateTimePicker · perawat=sesi login · auto-fill template). **Pemeriksaan Fisik ✅ IGD (2026-06-14, Domain 10 A+B+C)** — `medicalrecord.PemeriksaanFisik` append-only "latest wins" + `/kunjungan/:id/pemeriksaan-fisik` (GET/POST); **sub Anatomi = `medicalrecord.PenandaanAnatomi`** (daftar hidup body-map, `/kunjungan/:id/penandaan-anatomi` GET/POST + `/:itemId` PATCH/DELETE — toggle area→create/soft-delete, catatan→update); gate **`clinical.pemeriksaan`** (r/c/u/d Dokter+Perawat); meta waktu=DateTimePicker · dokter=roster ruangan (`/kunjungan/:id/petugas`) · perawat=sesi login; +Mobilitas +Catatan generalis +Temuan manual; IGD head-to-toe (RI/RJ per-sistem ditunda → TECH_DEBT). **Sub Penunjang ✅ (2026-06-15, Domain 10 Fase D)** — difokuskan ke diagnostik bedside **non-Lab/Rad** (Lab/Rad/USG dihapus → punya modul sendiri; sisa EKG + Spirometri/EEG/EMG/Audiometri/Ekokardiografi/Treadmill); form interpretatif (Keterangan+Hasil+Kesimpulan, tanggal=DateTimePicker); domain `medicalrecord.PemeriksaanPenunjang` (add/delete only soft-delete, `/kunjungan/:id/pemeriksaan-penunjang` GET/POST + `/:itemId` DELETE, gate `clinical.pemeriksaan`); upload file masih placeholder (butuh storage). **Penilaian / Assessment (Domain 11) 🚧 IGD (2026-06-15)** — tab Penilaian = banyak sub-menu, **tiap sub-menu = tabel `penilaian_*` sendiri** (prefix `penilaian_`, BUKAN Observation); gate **`clinical.penilaian`** (r/c append-only). **Sub-menu 1 Fisik ✅** — `medicalrecord.PenilaianFisik` (append-only, pem. umum + KU/Kesadaran/Gizi/Mobilitas) + `/kunjungan/:id/penilaian-fisik` GET/POST, wired IGD (panel Riwayat + `SaveBtn` onClick/loading). **Sub-menu 2 Nyeri ✅** — `medicalrecord.PenilaianNyeri` = asesmen komprehensif PQRST (lokasi/karakter/tipe/durasi/faktor/dampak/reasesmen); **keputusan: skor NRS single source = TTV/Observation** (5th vital sign per shift) → panel Nyeri tampilkan skor TTV terkini **read-only** (baca `listObservasi`), TIDAK simpan angka tandingan (hindari drift); `/kunjungan/:id/penilaian-nyeri` GET/POST, wired IGD. **Sub-menu 3 Status ✅** — `medicalrecord.PenilaianStatus` (status/kesadaran/catatan, append-only) + `/kunjungan/:id/penilaian-status` GET/POST (gate `clinical.penilaian`), wired IGD (mock `STATUS_NOTES` dihapus → riwayat dari DB). **Sub-menu 4 Pediatrik ✅** — `medicalrecord.PenilaianPediatrik` (beratLahir/usiaGestasi/imunisasi/tumbuhKembang/catatan, append-only) + `/kunjungan/:id/penilaian-pediatrik` GET/POST, wired IGD. **Diagnosis DI-DROP (2026-06-15)** — redundan dgn tab Diagnosa (ICD terkode, single source billable) + CPPT (rencana=SOAP P); sub-tab dihapus, tanpa `penilaian_diagnosis`. **Asesmen Risiko ✅ (2026-06-15)** — Morse/Braden/Barthel + 8 skala lain TIDAK lagi hardcode; **dikonsolidasi jadi 1 sub-tab generik "Asesmen Risiko" digerakkan master** (`master.SkalaInstrument` ter-assign unit via `konsumenModul`): `GET /api/v1/master/skala-tersedia?modul=` (gate `clinical.penilaian:read`, pola `sdki-template`) + render generik (items×opsi + skor live + interpretasi, sadar `arah`/`scoringMode`) + simpan snapshot ke **`medicalrecord.PenilaianSkala`** (1 tabel `penilaian_skala` utk semua skala skoring, append-only; `GET/POST /kunjungan/:id/penilaian-skala`); primitives diekstrak ke `igd/tabs/penilaian/shared.tsx` + [SkalaRisikoPanel](src/components/igd/tabs/penilaian/SkalaRisikoPanel.tsx). Sisa: **Jantung/Kanker** = Skala Penyakit (master kategori Penyakit belum di-seed → tetap hardcode). **Penandaan Gambar BE ✅ (2026-06-15, Domain 12)** — FE pakai **citra anatomi nyata PNG** (pria/wanita, anterior, `/public/anatomy`) + alat **Pin & Draw** (coretan merah) + zoom/geser; persist via **`medicalrecord.PenandaanGambar`** (daftar hidup per-item: koordinat % + `path` JSONB coretan + region/label/severitas snapshot; add/delete-only soft-delete, tanpa edit) + `/kunjungan/:id/penandaan-gambar` GET/POST + `/:itemId` DELETE (layered, gate **`clinical.pemeriksaan`** reuse — body-map = dokumentasi pemeriksaan, selaras PenandaanAnatomi). **Wired IGD ✅** — muat saat mount (UUID-guard), simpan per-item POST, hapus DELETE, Reset=bulk-delete per model; pasien demo (non-UUID) tetap lokal; "Simpan Semua" dihapus (persist per-item). **Serah Terima/Handover ✅ (2026-06-16, Domain 13)** — `medicalrecord.SerahTerima` closed-loop SBAR (tambah=INSERT · **terima=UPDATE-stamp sekali** · hapus=soft-delete; gate `clinical.keperawatan`; **TTV tidak disimpan** single-source Observation; penerima=actor server; wired shared IGD/RI). **Selesaikan Kunjungan / Disposisi ✅ (2026-06-16, Domain Outcome)** — `medicalrecord.Disposisi` (jenis Pulang/Rawat_Inap/Rujuk/Meninggal/APS) ditulis **atomik** via transisi `complete` (gate ≥1 Diagnosa Utama Pasti → `selesaiAt` efektif + `selesaiPertamaAt` immutable + `lockedAt`); **lock nyata** di `route()` choke-point (tolak tulis `clinical.*` saat terkunci, baca lolos; **pengecualian `allowWhenLocked` per-rute** utk dokumen administrasi kepulangan — resume medik/jadwal kontrol/pengembalian — 2026-07-05) + FE blur/disable (**tab Pasien Pulang dikecualikan** dari blur, banner emerald); **Batal Selesai** (`reopen` + `alasanReopen`, timestamp dipertahankan); authz klinis (Dokter/Perawat); 2 pintu (tombol header SelesaikanDialog + tab Pasien Pulang); board **"Selesai Hari Ini"** → rekam medis read-only. Roadmap [TODO-SELESAI-KUNJUNGAN.md](TODO-SELESAI-KUNJUNGAN.md). |
| [TODO-CLINICAL-RI.md](TODO-CLINICAL-RI.md) | **Rekam Medis Klinis Rawat Inap (DB-wiring tab) roadmap** — sambungkan 23 tab rekam RI ke DB agar pasien NYATA bisa mulai pengisian. **RI-CL0 fondasi ✅** (resolver+adapter+SessionProvider+invarian CHECK). Fokus = **menyambung** (mayoritas domain `medicalrecord.*` sudah ada dari IGD), bukan bikin domain. Tracker 23 tab (19🟢 · 4⬜; **Pasien Pulang RI 🟢 (2026-07-05)**: sub disposisi = **reuse form IGD** via tipe sempit `PulangPatient` + adapter (bukan fork) + prop `excludeStatus` sembunyikan Rawat_Inap/SPRI; urutan sub-tab dipindah tepat sebelum Resume Pulang; StatusPane lama dihapus; **`onComplete` wired ✅** — RIRecordResolver pegang lifecycle → `transitionKunjungan complete` (Disposisi+lock+release bed) + RIRecordTabs blur lock (tab Pasien Pulang dikecualikan) + SembuhPanel obat pulang AUTO dari order `isObatPulang` (daftar hijau); **sub Obat & Jadwal redesign ✅** — FKTP dihapus · Obat Pulang = order resep NYATA ke Farmasi ber-flag **`isObatPulang`** (kolom additif migrasi `20260703140000` + badge "Obat Pulang" worklist Farmasi; katalog formularium + depo master + status chip order terkirim) · Jadwal Kontrol PERSIST (`medicalrecord.JadwalKontrol`+counter `JK-<YYMM><NNN>`; BPJS → konektor `insertRencanaKontrol` mock always-success → noReferensi=noSuratKontrol; kodeDokter resolve server dari `bpjs.DpjpMapping`; picker SEP terbit `/kunjungan/:id/sep-terbit`; panel "Surat Kontrol Terbit"; **Edit Jadwal ✅ (2026-07-06)** — `PATCH /kunjungan/:id/jadwal-kontrol/:itemId` (`JadwalKontrolEditInput`; DAL `update`+DTO expose `dokterId`; baris BPJS ber-noReferensi → konektor `updateRencanaKontrol` **RencanaKontrol/Update** PUT mock always-success DULU (R4) lalu update DB; **noSuratKontrol/nomor/noSep TETAP** = revisi bukan terbit-baru; kodeDokter re-resolve server; `allowWhenLocked`) + FE tombol Pencil per kartu → form mode-edit (prefill, nomor/ref/SEP read-only "Tetap", payload `RencanaKontrol/Update` ber-noSuratKontrol)) · **Kembalian Obat DB-wired** (`medicalrecord.PengembalianObat`+Item, RBAC BARU `clinical.pengembalian`, Draft→Diverifikasi stamp sekali, verifikasi HANYA Apoteker di Service, buat dokumen dari order resep DB) · **Cetak Surat Kontrol ✅ (2026-07-04)** — sub Surat-surat: kartu Surat Kontrol tombol Cetak NYATA → [SuratKontrolCetakModal](src/components/rawat-inap/pasienPulang/SuratKontrolCetakModal.tsx)+Template A4 KOP RS (pola ResepCetakModal; data `listJadwalKontrol` DB — nomor JK + No. Referensi BPJS + No. SEP; picker chip multi-jadwal, 1 surat per jadwal; demo → mock) · **Resume Medik DB-wired + TTE QR ✅ (2026-07-04)** — domain BARU `medicalrecord.ResumeMedik` (migrasi `20260704120000`, append-only latest-wins: narasi DPJP + `dataKlinis` JSONB snapshot + TTE stamp-sekali per revisi, sign HANYA Dokter di Service) + `GET/POST /kunjungan/:id/resume-medik` + `/sign`; agregat DB via [resumeMedikAggregates](src/components/rawat-inap/pasienPulang/resumeMedikAggregates.ts) (TTV pertama/terakhir · diagnosa+prosedur+tindakan · obat resep · lab abnormal H/L/C · kesan rad); cetak A4 KOP RS + **QR TTE** [ResumeMedikCetak](src/components/rawat-inap/pasienPulang/ResumeMedikCetak.tsx) · semua kontrol global Select/DatePicker · **Discharge Planning ✅ 3/3 step (Asesmen 2026-07-02 · Edukasi+Checklist 2026-07-03)**: `medicalrecord.DischargeAsesmen` latest-wins + `/kunjungan/:id/discharge/asesmen`; `medicalrecord.DischargeEdukasi` log per topik (add + soft-delete, snapshot topik/kategori, petugas=sesi login server-otoritatif) + `/kunjungan/:id/discharge/edukasi` (+`/:itemId` DELETE=action update korektif); `medicalrecord.DischargeChecklist` snapshot dokumen latest-wins (items JSONB penuh + catatan DPJP) + `/kunjungan/:id/discharge/checklist` + Simpan Checklist; form global Select/DatePicker · Consent terverifikasi wired · **Konsultasi ✅ 2026-07-02**: domain BARU `medicalrecord.Konsultasi` closed-loop SBAR — peminta (tab shared wired `/kunjungan/:id/konsultasi` create/selesai/batal) → **worklist konsultan halaman RJ** ([KonsultasiInbox](src/components/rawat-jalan/KonsultasiInbox.tsx)) → halaman jawab fokus `/rawat-jalan/konsultasi/:id` (Terima→Jawab, endpoint standalone `/konsultasi` `scopeKunjungan:false` lintas careUnit) → **jawaban auto-CPPT** ke kunjungan asal (tx sama) → Konfirmasi Selesai DPJP; RBAC BARU `clinical.konsultasi` (Dokter CRUD · Perawat read, migrasi `20260702130000`); DetailPane 3 mode demo/peminta/konsultan · **MAR ✅ 2026-07-02**: domain BARU `medicalrecord.MarEntry` append-only latest-wins per obat×tanggal×shift, baris obat derivasi `ResepItem` non-batal + snapshot medikolegal, `GET/POST /kunjungan/:id/mar` gate `clinical.keperawatan`, perawat=user login server-otoritatif, HAM wajib verifikator ke-2 di Service; **Verifikasi Identitas RI** kini `defaultPerawat` dari sesi login): **Kat. A sambung** (Pemeriksaan/Pasien Pulang/Konsultasi) · **Kat. B domain baru** (Care Plan/Intake-Output/ICU Scoring/Gizi/Discharge/MAR/Konseling). Urutan by frekuensi bangsal. Aturan main = [docs/RAWAT-INAP-RECORD-RULES.md](docs/RAWAT-INAP-RECORD-RULES.md) (R1–R10+DoD). **Care Plan / Rencana Asuhan ✅ (2026-07-01)** — domain BARU `medicalrecord.CarePlanMasalah`+`CarePlanGoal` (RAT **Goal-centric & problem-oriented**: parent masalah link Diagnosa/SDKI via `sumber`+`refKode` anti-redundan, anak goal terukur per PPA = data baru; co-sign DPJP per-masalah, gate **`clinical.careplan`** BARU verify khusus Dokter di Service) + `/kunjungan/:id/care-plan` (+`/:masalahId`, `/goal`, `/goal/:goalId`) layered + [CarePlanTab](src/components/rawat-inap/tabs/CarePlanTab.tsx) rewrite (UUID guard; demo lokal; PhaseSection hardcode dihapus). |
| [TODO-SELESAI-KUNJUNGAN.md](TODO-SELESAI-KUNJUNGAN.md) | **Fitur "Selesaikan Kunjungan" (IGD Disposisi + lifecycle lock)** — ✅ **SELESAI** (2026-06-16). `medicalrecord.Disposisi` atomik via transisi `complete` (gate Diagnosa Utama + lock nyata + reopen/Batal Selesai). Fase 1–4 ✅. |
| [TODO-INVENTORY.md](TODO-INVENTORY.md) | **Inventory (Logistik) — modul BARU** `/ehis-inventory`. Layer stok operasional di atas master Obat+BMHP (movement-ledger source of truth). 7 menu: Beranda · Daftar Barang · Stok Opname · Pengiriman · Rekanan · Monitoring · Distribusi. **Distribusi Obat di Mapping Hub dihapus** (digantikan). Urutan: doc ✅ → FE (mock-first, SaaS, accent cyan / no purple) ✅ → Backend ✅. |
| [TODO-INVENTORY-BACKEND.md](TODO-INVENTORY-BACKEND.md) | **Inventory Backend roadmap — ✅ BE0–BE8 SELESAI** (2026-06-19). Schema `inventory` (12 model: ledger `StockMovement` + proyeksi `StockBatch`/`StockBalance` + Vendor/GoodsReceipt/Transfer/Distribusi/Opname + counter) + RBAC `inventory.*`. Prinsip: **ledger=truth · saldo=proyeksi · batch+ED FEFO · item=`itemJenis`+`itemId` soft-ref**. Primitif tunggal `movementService.postMovement` (anti-negatif). **BE1** core ledger · **BE2** Daftar Barang (read+seed dari edge Ketersediaan Farmasi) · **BE3** Vendor CRUD · **BE4** Penerimaan→IN · **BE5** Transfer+Distribusi (2-fase reservasi `qtyReserved`→TRANSFER) · **BE6** Opname→OPNAME per selisih · **BE7** Beranda+Monitoring agregat. Semua 7 menu FE ter-swap mock→DB. Follow-up: Vendor edit/delete UI · partial-fulfill · distribusi→unit (OUT) · dispensing Farmasi→OUT + billing. |
| [TECH_DEBT.md](TECH_DEBT.md) | Tech debt registry per-modul + cross-cutting. |
| [TODOS_BACKEND.md](TODOS_BACKEND.md) | Backend implementation roadmap (B0–B4, ~5–7 bulan). |
| [TODO-CHARGEMASTER.md](TODO-CHARGEMASTER.md) | **Chargemaster / Billable-Service federation (Opsi A)** — Fase CM0–CM5. Pisah katalog klinis vs layer tarif/billable; federasi Tindakan+Lab+Rad → Mapping Hub Tarif + Layanan Unit + billing. 📋 Planned. |
| [docs/BACKEND-MASTER-KATALOG-KLINIS.md](docs/BACKEND-MASTER-KATALOG-KLINIS.md) | **Backend grup Katalog Klinis** — Tindakan ✅ · ICD ✅ · Obat ✅ · Lab ✅ · Keperawatan/SDKI ✅ · Rad ✅. |
| [docs/BACKEND-MASTER-TEMPLATE&ENUM.md](docs/BACKEND-MASTER-TEMPLATE&ENUM.md) | **Backend grup Template & Enum** — 3 tab: **Status Enum ✅ (TE1)** · **Template Anamnesis ✅ (TE2, 2026-06-30)** · Template Form 📋 (TE3). Vertical slice per tab, gate `master.konfigurasi` + konsumsi klinis `clinical.rekammedis` (`*-tersedia`). |
| [docs/BACKEND-MAPPING.md](docs/BACKEND-MAPPING.md) | **Backend Mapping Hub** (`/ehis-master/mapping`) — status per sub-pane: SDM ✅ · Layanan Unit ✅ · RBAC ✅ · Tarif ✅ · Formularium ✅ · Kewenangan 🟡 · Distribusi/Penjamin 📋. |
| [docs/BACKEND-FLOWS.md](docs/BACKEND-FLOWS.md) | **Kontrak backend INTI** — layering · error · data rules · API · DoD (_mengapa_). Menang bila konflik. |
| [docs/API-RULES.md](docs/API-RULES.md) | **Resep endpoint konkret** — pola `route()`, error handling, peta file kanonik (_bagaimana_ menulis endpoint). Baca sebelum tambah/ubah API. |
| [docs/ASSIGNMENT-RULES.md](docs/ASSIGNMENT-RULES.md) | **Hybrid Authz (RBAC + SDM Assignment ABAC)** — aksi tulis unit penunjang "hanya petugas ter-assign": RBAC di route() + cek `pegawaiId ∈ roster` di Service; bypass `isSuperuser\|\|isGlobal`; nama entitas-terpilih diturunkan dari roster (anti-spoof). Baca sebelum aksi tulis Lab/Rad/Farmasi. |
| [docs/RAWAT-INAP-RECORD-RULES.md](docs/RAWAT-INAP-RECORD-RULES.md) | **Aturan kanonik rekam medis Rawat Inap (DB-wiring tab klinis)** — R1–R10: tabel `medicalrecord.*` unit-agnostic (key `kunjunganId`, **tanpa kolom unit**) · bedakan unit via props/adapter (jangan fork shared) · identitas = kunjungan UUID + guard `UUID_RE` (DB vs demo) · backend berlapis + gate `clinical.*`+careUnit, reuse domain IGD · migrasi additive anti-drift + **partial CHECK invarian unit** (`kunjungan_*_chk`) · hormati lock · sinkronkan tracker. **Tracker status tab RI (✅/🟡/❌) + DoD per-tab + urutan kerja.** Baca sebelum menyentuh tab rekam RI / menyambungnya ke DB. |
| [.claude/DONE.md](.claude/DONE.md) | Completed work archive (history per modul). |
| [.claude/GAP_ANALYSIS.md](.claude/GAP_ANALYSIS.md) | Clinical gap audit (SNARS/PMK/ISO). |
| [.claude/STANDARDS.md](.claude/STANDARDS.md) | Clinical standards reference. |
| `@.claude/skills/frontend-design/SKILL.md` | Frontend design skill. |

---

## 🛠 Stack

Next.js 16.2.3 App Router · React 19.2.4 · TypeScript 5 · Tailwind v4 (`@tailwindcss/postcss`) · Framer Motion 12 · Lucide React 1.8 · Prisma 7.7 (generated at `src/generated/prisma/`) · ESLint 9

**Convention:** `cn()` di `src/lib/utils.ts` · Navigation di `src/lib/navigation.ts` · Mock data klinis di `src/lib/data.ts` · Mock master di `src/lib/master/*Mock.ts` · Shared medical-records di `src/components/shared/medical-records/` · Master Template Layer di `src/components/master/shared/`.

---

## 📦 Module Map

| Route                      | Module           | Status                          |
| -------------------------- | ---------------- | ------------------------------- |
| `/ehis-care/igd`           | IGD              | ✅ 100% (19 tab aktif)          |
| `/ehis-care/rawat-inap`    | Rawat Inap       | ✅ 100% (20 tab aktif — **+Tindakan ✅ 2026-07-13**) · **Landing DB-driven ✅ (2026-06-28)** — [RawatInapPageView](src/components/rawat-inap/RawatInapPageView.tsx) orkestrasi census (`GET /kunjungan?unit=RawatInap` aktif → adapter `KunjunganListItemDTO`→RawatInapPatient: ruangan/bed/DPJP diresolusi dari master `getTree`+`listDokter`+alokasi) + statistik nyata (BOR/bed terisi-tersedia/pasien/bangsal). **Informasi Tempat Tidur** dirombak (bukan collapse): [RIBedInfoPanel](src/components/rawat-inap/RIBedInfoPanel.tsx) ringkasan per-kelas + [RIBedMapModal](src/components/rawat-inap/RIBedMapModal.tsx) peta bed semua bangsal (referensi read-only, okupansi nyata Tersedia/Terisi+pasien/Dipesan/Nonaktif dari `listActiveBedAllocations`; **klik kartu kelas → peta terbuka pra-filter kelas itu**, tombol "Peta Tempat Tidur" = semua). **Semua mock landing dihapus** (`rawatInapPatients`/`rawatInapRuangan`/`rawatInapStats` + `RIRuanganPanel` collapse). Detail `[id]` pasien NYATA → **rekam medis 19-tab penuh ✅ (2026-06-29)**: [RIRecordResolver](src/components/rawat-inap/RIRecordResolver.tsx) (pola IGDRecordResolver) fetch `GET /kunjungan/:id`+`/patients/:id`+master (tree/alokasi/Dokter) → adapter [riDetailApi](src/components/rawat-inap/riDetailApi.ts) `dtoToRawatInapPatientDetail` (klinis kosong, ruangan/bed/DPJP/kelas diresolusi) → render [RIPatientHeader](src/components/rawat-inap/RIPatientHeader.tsx)+[RIRecordTabs](src/components/rawat-inap/RIRecordTabs.tsx); kartu census (klik) buka rekam → **mulai pengisian** (tab DB-wired CPPT/TTV/Diagnosa/… persist by kunjungan UUID via `kunjunganId=patient.id`). Layout `[id]` di-wrap `SessionProvider` (klinis pakai useSession). `RIDetailFallbackClient` (ringkasan admisi sementara) **dihapus** — disuperseksi resolver. **Terima Order ✅ (2026-06-29)** — alur 2-leg: admisi daftarkan RI via SPRI + reservasi bed (status `Registered`), lalu bangsal **Terima Order** (pola IGD). [RawatInapPageView](src/components/rawat-inap/RawatInapPageView.tsx) pisah worklist: `Registered` → [RIOrderInbox](src/components/rawat-inap/RIOrderInbox.tsx) ("Order Masuk — Menunggu Diterima", kartu DPJP/ruangan/bed-reservasi/diagnosa/SEP + tombol **Terima Order**); `InService` → census. Terima = `PATCH /kunjungan/:id/status {action:receive}` (gate klinis `clinical.rekammedis:update`, no bedId) → backend **`bedAllocationService.occupyReserved`** flip reservasi bed `Reserved→Occupied` in-place (hindari create ganda P2002; IGD tetap occupy bed pilihan saat terima) + status `Registered→InService` → refetch → pasien pindah ke census + bed terisi. **Gate akses rekam medis ✅ (2026-07-13)** — resolver **RI & IGD & RJ** menolak buka rekam kunjungan pra-terima (**`Registered`**; RJ juga **`Queued`** — dipanggil ≠ diterima) & `Cancelled` → layar [RecordGateScreen](src/components/shared/RecordGateScreen.tsx) (amber "Order Belum Diterima" + petunjuk Terima per unit / rose "Dibatalkan"), menutup jalur URL langsung + "Buka Rekam Medis" registrasi; guard BE tulis-saat-Registered = TECH_DEBT. **Batalkan Order ✅ (2026-06-29)** — tombol kedua di kartu order + **dialog konfirmasi** ([RICancelOrderDialog](src/components/rawat-inap/RIOrderInbox.tsx), rose, sebut bed yang dilepas) → `{action:cancel}` (Registered→Cancelled) + `bedAlloc.release` (reservasi dilepas, bed tersedia kembali). **Authz cancel kini status-aware**: `assertTransitionAllowed` dipindah ke dalam tx (akses `k.status`) — batal **order belum dilayani** (`Registered`) boleh aktor klinis (`clinical.rekammedis:update`), batal pasca-pelayanan (Queued/InService) tetap loket (`registration.kunjungan:update`). Busy per-aksi `{id,action}`. **SPRI dipulihkan saat batal ✅ (2026-06-29)** — cabang `cancel` panggil `spriDal.revertConsumed` (SPRI ber-`riKunjunganId`=kunjungan dibatalkan & `Dikonsumsi` → balik `Terbit`/`MenungguRef` + lepas `riKunjunganId`) → order valid lagi di **worklist admisi + dashboard pasien** (re-admisi). **Anti-admisi ganda: auto-tutup SPRI saudara ✅ (2026-06-29)** — `spriService.consume` kini atomik: konsumsi SPRI + `spriDal.supersedeSiblings` (SPRI lain milik pasien sama via `kunjungan.patientId`, status aktif → **`Tergantikan`** — status SPRI baru) → hilang dari worklist (default filter `[MenungguRef,Terbit]`) sehingga 1 pasien tak bisa diadmisi 2× dari SPRI berbeda. Catatan: belum ada guard "1 admisi RI aktif" di registrasi (SPRI baru pasca-admisi belum tertutup); saudara tak dipulihkan saat batal. **Revisi & Batalkan SPRI (WS BPJS) ✅ (2026-06-29)** — worklist [AdmisiRanapBoard](src/components/registration/admisi/AdmisiRanapBoard.tsx) dapat 2 aksi baru: **Revisi SPRI** ([SpriEditModal](src/components/registration/admisi/SpriEditModal.tsx) — edit DPJP/tgl/jenis/indikasi/ket) → `PATCH /spri/:id` → server kirim **RencanaKontrol/UpdateSPRI** ke BPJS (bila sudah ada No. Referensi) lalu update lokal; **Batalkan SPRI** (SpriCancelDialog) → `DELETE /spri/:id` → **RencanaKontrol/Delete** ke BPJS + status `Batal`. Konektor `services/bpjs/rencanaKontrol.{updateSPRI,deleteSPRI}` (mock always-success, audited; sandbox PUT/DELETE) + wrapper `spriBpjsMock.{updateSpriRef,cancelSpriRef}` (resolve kodeDokter dari `dpjpPegawaiId`). WS gagal → `Errors.validation` dgn `details.spriReject` (pola SEP) → toast `BPJS <code>: <message>`. Guard: SPRI `Dikonsumsi` tak bisa edit/batal (batalkan via Batalkan Order RI). Layered Zod `EditSpriInput`/DAL `updateFields`+`cancel`/Service/route/API client. **TITIPAN + konfirmasi kelas ✅ (2026-06-29)** — kolom additif `kunjungan.{kelasHak,titipan,titipanAlasan}` (migrasi `20260629120000`); `kunjungan.kelas` kini = **kelas KAMAR** (ruangan terpilih, `form.kelasKamar` di StepKunjunganRi — bukan tombol "Kelas Rawat" lagi). Saat admisi **BPJS** & kamar ≠ hak kelas → [KelasMismatchDialog](src/components/registration/patient/modals/daftar-kunjungan/KelasMismatchDialog.tsx) (Titipan / Beda-kelas + alasan); Titipan → `titipan=true`, `kelasHak`=hak (basis tagihan). Badge **TITIPAN** (amber) di kartu order inbox + census [RIBoard](src/components/rawat-inap/RIBoard.tsx). Follow-up: status klinis board (Kritis/Observasi) belum terderivasi worklist · sebagian tab detail belum DB-wired (asesmen awal/care-plan/isolasi masih mock UI) · Terima opsional ganti bed · **billing belum baca `kelasHak`** (akomodasi titipan = sync nanti) |
| `/ehis-care/rawat-jalan`   | Rawat Jalan      | ✅ 100% (14 tab aktif — **+Tindakan ✅ 2026-07-13**) · **Landing bertab ✅ (2026-07-02)** — [RJPageView](src/components/rawat-jalan/RJPageView.tsx) (pola LabPageView): **Order Masuk | Worklist Pasien | Konsultasi** dengan badge angka + pulse merah kedip (order/konsul masuk belum diterima); tombol "Kunjungan Baru" dihapus (pendaftaran = loket); `RJBoardLive` diserap ke RJPageView (badge tahu jumlah tanpa buka tab). **Filter periode tanggal ✅ (2026-07-12)** — bar Dari/Sampai (`DatePicker`) + preset Hari Ini/7 Hari/Bulan Ini + Reset di baris tab (worklist/order saja) → filter **server-side** by `waktuKunjungan` inklusif: `WorklistQuery.{dari,sampai}` (Zod `YYYY-MM-DD`) → Service parse UTC-day (00:00:00.000Z–23:59:59.999Z) → DAL `listByUnitStatus` `waktuKunjungan {gte,lte}`; kosong = tampilan live; rentang selalu valid (clamp menyilang); ringkasan rentang + jumlah kunjungan. **Panel Poliklinik difilter SDM Assignment** user login via endpoint self-scoped `GET /master/poli-saya` (gate `clinical.rekammedis:read`, mapper strict `rjPoliStrict` tanpa fallback Poli_Umum; superuser/global/tanpa-penugasan-poli → semua poli) · **Surat & Dokumen DB+TTE ✅ (2026-07-09)** — Surat Ket. Sakit (`medicalrecord.SuratKeteranganSakit` + `/kunjungan/:id/surat-sakit`, counter `SKS-<YYMM><NNN>`, TTE QR dokter, template A4) · Surat Ket. Sehat/SKBS (`medicalrecord.SuratKeteranganSehat` + `/surat-sehat`, BMI auto) · Resume Medik RJ (QR TTE **hanya di cetakan**, kartu hijau "sudah di-TTE") · **Riwayat pramuat** 3 surat (Kontrol/Sakit/Sehat) saat tab dibuka. **Disposisi Rujuk Eksternal JKN ✅ (2026-07-09)** — Rujuk Internal dihapus (tinggal Rujuk Eksternal + Admisi RI); form JKN = payload resmi V-Claim `Rujukan/insert` (t_rujukan) [RujukJknForm](src/components/rawat-jalan/tabs/disposisi/RujukJknForm.tsx): faskes `listSarana`/poli `listSpesialistik` via `Select` portal (anti-clip) · tanggal `DatePicker` (tanpa default) · SEP autofill `listSepTerbit` · diagnosa `getDiagnosa` · `user`=login · pratinjau payload hyperlink→modal; **kirim SELALU sukses (mock, belum cons-id prod)** → sintesis respons detail rujukan → **Surat Rujukan cetak A4** [RujukanCetakTemplate](src/components/rawat-jalan/tabs/disposisi/RujukanCetakTemplate.tsx) (KOP RS · Dirujuk Ke · No. Rujukan `{PPK}{MMYY}B{6}` · berlaku +90h); **DB-wired ✅ + Cetak Ulang** — persist `medicalrecord.RujukanEksternal` (`POST/GET /kunjungan/:id/rujukan` + `/:itemId` DELETE, snapshot `detail` JSONB, counter `RUJ-<YYMM>`, layered) → **widget hijau "Surat Rujukan Sudah Terbit"** [RujukanKeluarWidget](src/components/rawat-jalan/tabs/disposisi/RujukanKeluarWidget.tsx) (di bawah Jenis Disposisi saat Rujuk Eksternal dipilih: detail surat terbaru + Cetak + Batalkan soft-delete + surat lain collapsible); issuance BPJS masih mock (No. Rujukan = counter server). Non-JKN → RujukUmumForm. **Disposisi Admisi Rawat Inap = penerbitan SPRI (form IDENTIK IGD) ✅ (2026-07-11)** — sub-menu "Admisi Rawat Inap" (dulu form mock alasan/kelas/diagnosa yang tak persist) kini render **[SpriIssuePanel](src/components/shared/spri/SpriIssuePanel.tsx)** = pembungkus [SPRIPanel](src/components/igd/tabs/pasienPulang/SPRIPanel.tsx) IGD (roster DPJP dari `listDokter`); submit → `createSpri` tautkan SPRI ke kunjungan RJ berjalan (UUID) → muncul di Worklist Admisi Registrasi (demo non-UUID → tak persist); SuccessScreen tampilkan No. Referensi. **Layout 2-panel**: kiri form, kanan **Riwayat SPRI** pasien ([SpriRiwayatPanel](src/components/shared/spri/SpriRiwayatPanel.tsx) via endpoint klinis BARU `GET /kunjungan/:id/spri` gate `clinical.rekammedis:read`; semua SPRI lintas kunjungan, refetch sesudah terbit). **Rekam Medis header ringkas + Selesaikan + Batalkan Selesai 2-mode ✅ (2026-07-11)** — [RJPatientHeader](src/components/rawat-jalan/RJPatientHeader.tsx) compact (No. SEP, 3 widget Antrian/DPJP/Masuk→Keluar, tanpa Alamat, data NYATA DB `selesaiAt`/`lockedAt`/`version`); gate "Diagnosa belum lengkap" reaktif via recordBus `emitRecordChange(kunjunganId,"diagnosa")`; Selesaikan → `transitionKunjungan complete` (SelesaikanDialog reuse IGD); **Batalkan Selesai (reopen) DB-wired high-alert** ([BatalSelesaiRJDialog](src/components/rawat-jalan/BatalSelesaiRJDialog.tsx)) 2 mode: **Perbaikan Pengimputan** (tgl keluar dipertahankan → **Selesaikan Kembali = kunci ulang LANGSUNG tanpa form**, BE `complete` tanpa disposisi = re-lock disposisi terakhir) / **Perbaikan Menyeluruh** (tgl keluar BARU → BE `resetSelesai` kosongkan `selesaiAt`, `selesaiPertamaAt` audit tetap, penyelesaian ulang via form disposisi); Zod refine "disposisi wajib" pindah ke Service (DB-state-aware); lock-blur RJRecordTabs KECUALI tab Disposisi & Surat/Dokumen. Detail → [.claude/DONE.md](.claude/DONE.md) |
| `/ehis-care/farmasi`       | Farmasi          | ✅ 100% FE (4 layer + Gap SNARS T1–T3). **Klinis BE ✅ (2026-06-20→21)** — halaman detail `/farmasi/:id` DB-driven; **Telaah** persist `medicalrecord.ResepTelaah` (PMK 72/2016 3-aspek = QuestionnaireResponse-ready, `POST /farmasi/resep/:id/telaah` gate `ancillary.farmasi.telaah`) + **Dispensing** persist `medicalrecord.ResepDispensing` (MedicationDispense-ready, gate `ancillary.farmasi.serah`), transisi `Diterima→Ditelaah→Selesai` atomik; **Riwayat Resep** lintas-kunjungan (filter `noRM`, status `Dibatalkan` kelas-satu); **Dokumen cetak** Resep/Label/Etiket (kertas thermal 58/80mm/A4 + `@page` dinamis); **worklist** depo=nama unit (`DEPO_LABEL`) + StatusFilter dropdown kustom; **CPPT Apoteker terintegrasi** (tulis ke `medicalrecord.Cppt`; grant Apoteker `clinical.cppt:[read,create,delete]` + `isAncillaryActor` bypass careUnit ABAC); **Rekonsiliasi Obat** tab shared `RekonsiliasTab` (konteks dari unit asal order IGD/RI, `clinical.rekonsiliasi`); **Register N/P read-only** (katalog dari master.Obat golongan, saldo/mutasi dari ledger Inventory, modal tulis dihapus). Follow-up: dispensing→OUT Inventory · SIPNAP report · RI/RJ ResepPane TTE/CPPT. Detail → [.claude/DONE.md](.claude/DONE.md) |
| `/ehis-care/laboratorium`  | Laboratorium     | ✅ 100% (Tier 1+2+3). **Order Lab BE ✅ (2026-06-21):** `medicalrecord.LabOrder`+`LabOrderItem` (tabel sendiri, mirror ResepOrder; migrasi `20260621160000`) · `POST/GET /kunjungan/:id/lab` (gate `clinical.tindakan`) + cancel · worklist `GET /lab/orders` + `/:id` + `/:id/receive` (gate `ancillary.lab.worklist`); item snapshot katalog (kode/nama/kategori/TAT/**harga** Tarif Matrix). Tab Order Lab shared submit saat kunjunganId UUID → tersimpan (`createLabOrder`). **Worklist DB-wired ✅ (2026-06-21):** `LabPageView` fetch `listLabWorklist` → `mapDbLabOrder`+`applyWorkflowOverlay` → `LabBoard` (props-driven); section **"Belum Diterima"** + Terima(`receiveLabOrder`); **notifikasi animasi angka order** di tab "Worklist Order" (badge spring + pulse) & StatCards (`AnimatedNum`); detail page DB (`LabOrderDetail` fetch `getLabOrder/:id`, `LabOrderTabs` props `order`+`onRefresh`); riwayat pasien dari DB (`listLabWorklist?noRM`). **Semua MOCK ORDER dihapus** (`LAB_ORDERS_BASE`/`deriveLabOrders`/`getLabOrderById`/`mergeOrder`); KategoriLab labShared diperluas 7→10. **Workflow detail 4→3 step ✅ (2026-06-21):** step "Pengambilan & Sampel" dihapus (sampel diambil di luar aplikasi) → Penerimaan → Entry Hasil → Validasi; `SampelPane` dihapus, `canEnter` HasilPane termasuk "Diterima", **Kriteria Penolakan Sampel** pindah jadi panel reminder "Pertimbangkan Kualitas Sampel" di Entry Hasil; `LAB_STATUS_STEPS`/step di-renumber (Timeline header juga buang Ambil/Reg. Sampel). **Penerima & analis = user login + Entry Hasil dari katalog ✅ (2026-06-21):** Penerimaan "Diterima Oleh" & "Analis Pelaksana" = `useSession().namaTampil` (read-only, SessionProvider di layout detail Lab); baris Entry Hasil = **parameter master.LabTest** tes diorder (rujukan/critical disesuaikan gender+usia pasien) via endpoint baru `GET /lab/test-params` (gate `ancillary.lab.worklist:read`, layered `findByIds`→`getByIds`); helper `buildHasilFromCatalog`/`hasilKey`, `LabOrderItem.labTestId`+`HasilItem.rowKey`. **Entry Hasil persist ✅ (2026-06-21):** `medicalrecord.LabResult`+`LabResultValue` (append-only "latest wins"; header analis+criticalNotifs JSONB + values per parameter; migrasi `20260621190000`) · `GET/POST /lab/orders/:id/hasil` (gate `ancillary.lab.worklist` read/update, `scopeKunjungan:false`) — POST simpan + transisi atomik order Diterima→Divalidasi; HasilPane fetch/POST DB (overlay tetap utk Validasi pane sefase). **Validasi persist ✅ (2026-06-21):** kolom validasi di `LabResult` (validator/catatan/validatedAt; migrasi `20260621200000`) + `POST /lab/orders/:id/validasi` (gate **`ancillary.lab.validate:update`** SpPK; stamp sekali guard `validated_at IS NULL` + transisi atomik Divalidasi→Selesai); ValidasiPane fetch hasil DB + billing ingest client. **Gating SDM Assignment — HYBRID RBAC+ABAC ✅ (2026-06-21):** roster Lab `GET /lab/orders/:id/petugas` (gate `ancillary.lab.worklist:read`; pegawai aktif ter-assign ke Location tipe Laboratorium via `penugasanRuangan`, order ber-`labKode`→ruangan spesifik else semua Lab; layered `listPetugasByLocations`→`labOrderService.listPetugas`) + hook FE `useLabRoster` (+`doctors`/`isAssigned`). **Enforcement SERVER-SIDE** (bukan UI saja) via [labAssignment.ts](src/lib/services/lab/labAssignment.ts) — `assertActorAssignedToLab` pada **receive** & **saveHasil** (penerima/analis HARUS ter-assign); `resolveValidatorNama` pada **validate** (validator = dokter ter-assign, FE kirim `validatorPegawaiId`, nama **diturunkan dari roster** anti-spoof). **Bypass `isSuperuser \|\| isGlobal`** (Admin tak terkunci; konsisten unit-scope ABAC). FE pelengkap: `AssignmentGuardBanner` + tombol disable (Penerimaan/Entry Hasil), dropdown dokter custom [ValidatorPicker](src/components/lab/ValidatorPicker.tsx) (listbox avatar+animasi, `value=pegawaiId`, Validasi, superuser opsional). Pola → [docs/ASSIGNMENT-RULES.md](docs/ASSIGNMENT-RULES.md). **Cetak Hasil ✅ (2026-06-21):** PrintPreviewModal (RiwayatPane) fetch hasil **dari DB** (`getLabResult`, fallback overlay) → **hanya parameter terisi** (filter `nilai`), validator/analis/catatan/waktu rilis dari DB; **QR TTE** validator (SpPK) via [TteQr](src/components/lab/TteQr.tsx) (matriks QR-style deterministik, pola TteBarcode resep; serial `TTE-LAB-…` + payload `EHIS-LAB|order|RM|validator|ts|serial`) + CSS print khusus tanda tangan; **kertas A4** (`@page size:A4 margin:14mm 12mm` + preview lembar WYSIWYG 210mm). **Follow-up:** terapkan pola sama ke Rad/Farmasi · billing ingest server-side · QR ter-scan (lib encoder). |
| `/ehis-care/radiologi`     | Radiologi        | ✅ 100% (Tier 1+2+3)            |
| `/ehis-master`             | Master Data      | ✅ 100% FE (26 sub-master + 8 mapping + Beranda) — incl. **Jadwal Dokter** (single source HFIS, dikonsumsi Antrean/RJ). **Backend: Pegawai + Pengguna ✅ (2026-06-05)** — `/api/v1/master/pegawai` (CRUD layered: list/detail/create/update PATCH+version/soft-delete; enum ASN/Outsourcing + kolom agama/profesi/punyaAkun) + `/api/v1/auth/users` (provisioning: create + assign roles + list; hash scrypt; seed 10 Role — **+Analis/ATLM Laboratorium 2026-06-21**, padanan Radiografer di sisi lab: grant `ancillary.lab.worklist`+`ancillary.lab.critical`, validasi tetap SpPK; migrasi `20260621170000`) **WIRED penuh ke tabel [/ehis-master/pengguna](src/app/ehis-master/pengguna/)** (Tambah Pengguna 3-step · baris kuning pegawai-tanpa-akun · Buatkan Akun · Ubah Data Pegawai). **Spesialistik + profesi Analis Lab ✅ (2026-06-21)** — grup Kepegawaian: profesi **"Analis Laboratorium"** ditambah ke `PROFESI_OPTS`; saat **Jenis Profesi = "Dokter Spesialis"** muncul dropdown **Spesialistik** (24 bidang, reveal beranimasi, required) di modal Ubah + wizard Tambah; kolom `master.Pegawai.spesialistik` (TEXT nullable, migrasi `20260621180000`) layered Zod/DAL/Service (gate by profesi efektif: non-spesialis → dipaksa null)/API. Kredensial klinis penuh (`Dokter.spesialisKode`/STR/SIP) **tetap di master Dokter** — field ini = deklarasi HR + prefill profil Dokter. **Unit & Ruangan ✅ (2026-06-06)** — `/api/v1/master/{unit,ruangan,bed}` (tree + CUD layered, optimistic concurrency, soft-delete) WIRED ke [/ehis-master/ruangan](src/app/ehis-master/ruangan/) (kode auto `UN/R/BD<YYMM><NNN>` · toast · ConfirmDialog · rekonsiliasi bed granular). **Dokter & Nakes ✅ (2026-06-06)** — `/api/v1/master/dokter` (CRUD layered; Dokter = ekstensi 1:1 Pegawai via `pegawaiId`, identitas read-only dari Pegawai, provisioning dari pegawai profesi-dokter "tanpa-profil") WIRED ke [/ehis-master/dokter](src/app/ehis-master/dokter/) (modal **"Lengkapi Profil"** gantikan tombol Tambah · SSR hybrid · STR/SIP expired badge). **SDM Assignment ✅ (2026-06-06)** — `/api/v1/master/penugasan-ruangan` (link N:M **Pegawai⇄Location**, hard-delete join table, idempoten) WIRED ke [Mapping Hub → SDM Assignment](src/components/master/mapping/sdm/) (tree Unit→Ruangan, assign **per-ruangan**, optimistik POST/DELETE + toast sukses/warning, SSR hybrid). **Mapping Hub SDM Assignment + Kewenangan Klinis kini konsumsi dokter REAL dari API** (semua mock dokter dihapus). **Pengguna + Ruangan + Dokter + Penugasan pakai SSR hybrid** (first paint via Service langsung, CUD client — API-RULES §6.1). **Auth AKTIF ✅ (2026-06-07, `AUTH_ENFORCE=true`)** — login/JWT/RBAC ditegakkan (getActor/getServerActor verifikasi token; superadmin Admin global). **RBAC modul + ABAC unit-scope ✅ (2026-06-11, RBAC-MODUL Fase 4)** — visibilitas modul/menu/aksi per-role; gate MODUL≠DATA (`registration.loket`/`master.view`); **ABAC careUnit** dari `Pegawai.unitKerja` ditegakkan 4 titik (nav · worklist · detail · choke-point `route()` utk semua `clinical.*`); penunjang Lab/Rad/Farmasi berdiri-sendiri via `ancillary.*`; rekam medis shared = `clinical.rekammedis`. Lihat [TODO-RBAC-MODUL.md](TODO-RBAC-MODUL.md) · [docs/BACKEND-AUTH.md](docs/BACKEND-AUTH.md) · [docs/BACKEND-FLOWS.md](docs/BACKEND-FLOWS.md#6-authentication--authorization--4-lapis) · [docs/API-RULES.md](docs/API-RULES.md) · [docs/BACKEND-MASTER-SUMBER-DAYA.md](docs/BACKEND-MASTER-SUMBER-DAYA.md) · [TODOS_BACKEND.md](TODOS_BACKEND.md#b11-sumber-daya). **Katalog Tindakan ✅ (2026-06-12)** — `/api/v1/master/tindakan` (CRUD layered, **leaf tanpa version**, kode ICD-9 **opsional**, **16 kategori**, status KPTL) WIRED SSR-hybrid ke [/ehis-master/katalog-tindakan](src/app/ehis-master/katalog-tindakan/) + **Mapping Hub → Layanan Unit** kini konsumsi **tindakan DB (baris) + unit dari Location aktif master Unit & Ruangan (kolom)** via SSR hybrid (`unitsFromTree`). **Katalog Laboratorium ✅ (2026-06-12)** — model **Tes→Parameter** (panel): `/api/v1/master/lab-test` (CRUD layered, `LabTest`+`LabParameter`, **rujukan numerik per-parameter = JSONB**, update **replace-all** parameter, **leaf tanpa version**) WIRED SSR-hybrid ke [/ehis-master/katalog-lab](src/app/ehis-master/katalog-lab/); form di-rewrite (tab **Parameter** + **Satuan combobox** satuan-baku + **DiscardDialog** + field **KODE dihapus**); **seeded 38 tes / 88 parameter** standar (PMK 43/2013 · NCEP · WHO · SAMHSA) via [prisma/scripts/seed-lab.mts](prisma/scripts/seed-lab.mts). Lab mock lama (`labCatalogMock.ts`) tetap utk HasilPane/TrendPane. **Lab → Layanan Unit ✅ (2026-06-12)** — Katalog Lab kini dipetakan ke Ruangan di **Mapping Hub → Layanan Unit** sebagai grup baris **"Tindakan Laboratorium"** (matriks terpadu Tindakan+Lab via `LayananRow`/`LayananEdge`); tabel **paralel** `master.LayananUnitLab` (LabTest⇄Location, join hard-delete, idempoten) + `/api/v1/master/layanan-unit-lab` (layered, RBAC `master.mapping`), SSR-hybrid. Bukan polymorphic — tetap tabel berdiri-sendiri sampai federasi chargemaster (TODO-CHARGEMASTER CM2/CM5). Matriks dapat **bulk select-all tri-state 3 level**: per kolom/Location · per baris · per grup kategori (desktop + mobile drill-down), semua batched optimistik. **Konsumen klinis ✅ (2026-06-12)** — `GET /api/v1/master/tindakan-tersedia` (read katalog tindakan ter-assign, gate **`clinical.tindakan:read`** bukan master, distinct + `ruanganKodes[]`, forward-ready `?ruanganKode=`) dipakai **tab Tindakan IGD** (redesign mirip tab Diagnosa: search-first + kartu konfigurasi + daftar tergrup + sidebar ringkasan; pelaksana default dari sesi login; Lab/Rad tereksklusi). Verifikasi identitas IGD kini ambil **nama verifikator dari user login** (`IdentitasVerifikasiBanner.defaultPerawat`). Status backend lengkap → [docs/BACKEND-MAPPING.md](docs/BACKEND-MAPPING.md). **Katalog Obat ✅ (2026-06-13)** — `/api/v1/master/obat` (CRUD layered, **leaf tanpa version**, enum FE-facing TEXT, harga Int, `lasaPairIds` text[], **pemetaan KFA = kolom JSONB** POA/POV/Rute/Bentuk+BZA utk interop FHIR SatuSehat) WIRED SSR-hybrid ke [/ehis-master/katalog-obat](src/app/ehis-master/katalog-obat/) (5 tab incl. **Mapping KFA** — search produk KFA → auto-fill mapping → preview FHIR `Medication`; LASA picker baca list DB; DiscardDialog). **`OBAT_MOCK` dihapus** → tipe+config tetap di `obatMock.ts`, data pindah ke [obatSeed.ts](src/lib/master/obatSeed.ts); **seeded 28 obat / 17 ter-KFA / 4 LASA** via [prisma/scripts/seed-obat.mts](prisma/scripts/seed-obat.mts) (derive KFA dari `kfaMock.ts`, remap lasaPairIds→UUID). Konsumen mock **dimigrasi off `OBAT_MOCK`**: Mapping Hub Formularium/Distribusi → `fetchAllObat` (DB) · Beranda → count indikatif · billing `priceResolver` → snapshot di-hydrate SSR layout billing. KFA search masih mock ([kfaMock.ts](src/lib/master/kfaMock.ts), swap ke BFF KFA v2 saat kredensial SatuSehat siap). Spec → [docs/BACKEND-MASTER-KATALOG-KLINIS.md §C.1](docs/BACKEND-MASTER-KATALOG-KLINIS.md). **Katalog BMHP/BHP ✅ (2026-06-19)** — katalog **TERPISAH dari Obat** (Bahan Medis Habis Pakai): `/api/v1/master/bmhp` (CRUD layered, **leaf tanpa version**, kode auto **`BHP-<YYMM><NNN>`** via `master.BmhpCounter`, enum FE-facing TEXT, harga Int, kolom `kfa` JSONB disiapkan tapi **KFA Alkes ditunda**) WIRED SSR-hybrid ke [/ehis-master/katalog-bmhp](src/app/ehis-master/katalog-bmhp/) (accent **teal**, ikon Syringe, **3 tab** Identitas/Klasifikasi/Harga, kode auto, DiscardDialog; gate `master.katalog`). Field alkes: ukuran/satuan/kemasan · steril/single-use/implan/kelas-risiko A-D · izin edar AKL/AKD + e-Katalog LKPP. Tipe+config di [bmhpMock.ts](src/lib/master/bmhpMock.ts), data di [bmhpSeed.ts](src/lib/master/bmhpSeed.ts); **seeded 18 item** `BHP-2606001..018` via [seed-bmhp.mts](prisma/scripts/seed-bmhp.mts). Konsumen hilir (Tindakan/billing/Formularium BMHP) = Phase later. Workflow → [docs/BACKEND-MASTER-KATALOG-BMHP.md](docs/BACKEND-MASTER-KATALOG-BMHP.md). **Formularium → DB ✅ (2026-06-13)** — Mapping Hub **Formularium** = grant N:N **Obat ⇄ Ruangan (Location)** persis Layanan Unit (matriks baris Obat × kolom Ruangan, **universal lintas penjamin** — tab penjamin & dimensi kelas dihapus). Persist via `master.FormulariumObat` + `/api/v1/master/formularium` (grant idempoten · hard-delete revoke · SSR-hybrid + optimistik + tree-filter kolom). Reuse helper grant-map/kolom-unit + `LayananUnitTreePanel` dari layananShared (cache edge terpisah). Spec → [docs/BACKEND-MAPPING.md §6](docs/BACKEND-MAPPING.md). **Ketersediaan Farmasi + BMHP ✅ (2026-06-19)** — sub-pane "Formularium" di-rename **Ketersediaan Farmasi** dengan segmented **Obat | BMHP**; sub BMHP = grant N:N **BMHP ⇄ Ruangan** ("daftar standar depo", bukan formularium klinis) via tabel TERPISAH `master.FormulariumBmhp` + `/api/v1/master/formularium-bmhp` (+`/:id`, idempoten · hard-delete · gate `master.mapping` · SSR-hybrid · accent **teal** · migrasi `20260619170000`); **kolom kedua sub-pane dibatasi Location jenis Farmasi/Gudang Farmasi**. Distribusi Obat (lama) sudah dihapus → modul `/ehis-inventory`. Konsumen hilir (Inventory picker per-lokasi · `bmhp-tersedia` klinis) = Phase later. Spec → [docs/BACKEND-MAPPING.md §6b](docs/BACKEND-MAPPING.md). **ICD-10/9 ✅ (2026-06-07)** — `/api/v1/master/icd` (+import bulk). Spec [docs/BACKEND-MASTER-KATALOG-KLINIS.md](docs/BACKEND-MASTER-KATALOG-KLINIS.md). **Katalog Keperawatan (SDKI/SLKI/SIKI) ✅ (2026-06-14)** — **rename** dari "SDKI / SIKI / SLKI" → [/ehis-master/katalog-keperawatan](src/app/ehis-master/katalog-keperawatan/); `/api/v1/master/sdki` (CRUD layered, **leaf tanpa version**, blok dataMayor/dataMinor/intervensi = **JSONB**, kriteriaHasil text[], **kode `D.NNNN` auto** via `master.SdkiCounter`) WIRED SSR-hybrid ke [SdkiPage](src/components/master/sdki/SdkiPage.tsx) (form Kode dihapus → display "Auto" + DiscardDialog). **`SDKI_MOCK` dihapus** → tipe+helper tetap di `sdkiMock.ts`, data pindah ke [sdkiSeed.ts](src/lib/master/sdkiSeed.ts); **seeded 27 diagnosa** via [seed-sdki.mts](prisma/scripts/seed-sdki.mts) (counter[D]=148 → baru D.0149). Konsumen klinis KeperawatanTab masih `SDKI_CATALOG` mock (follow-up). Spec → [docs/BACKEND-MASTER-KATALOG-KLINIS.md §C.3](docs/BACKEND-MASTER-KATALOG-KLINIS.md). **Skala Risiko ✅ (2026-06-15)** — model **1 tabel lintas-kategori** `master.SkalaInstrument` (kategori Risiko/Umum/Penyakit; items[]+interpretasi[] = JSONB, konsumenModul text[] = assignment unit IGD/RI/RJ/ICU) + counter kode **`SR-NNNN` auto** (`master.SkalaCounter`, BUKAN nama MORSE) + RBAC baru **`master.skala`** (r/c/u/d; Dokter/Perawat read); `/api/v1/master/skala-risiko` CRUD layered WIRED SSR-hybrid ke [SkalaRisikoPage](src/components/master/skala-risiko/SkalaRisikoPage.tsx) (kode **read-only "Auto"**, batal → **DiscardDialog** global, bukan window.confirm). **`SKALA_RISIKO_MOCK` di-seed ke DB — 13 instrumen tervalidasi** `SR-0001..0013` (counter=13) via [seed-skala-risiko.mts](prisma/scripts/seed-skala-risiko.mts); mock kini import type-only (Node-loadable). **Stratifikasi usia/kondisi (syarat JCI/SNARS "appropriate to age & condition")** — dewasa (Barthel/Morse/Braden/NRS/MUST/MST) + pediatrik (Humpty Dumpty/Wong-Baker/FLACC/Braden Q/STRONGkids) + neonatus (NIPS) + kritis-ICU (CPOT); tiap instrumen di-audit 1:1 vs publikasi asli (item·bobot·cut-off). Skala anak/neonatus map ke IGD/RI/RJ (enum `konsumenModul` belum punya NICU/PICU → ditunda). Konsumen klinis (tab Penilaian tarik dari master per `?modul=`) = follow-up. Spec → [docs/BACKEND-MASTER-SKALA-KLINIK.md §B](docs/BACKEND-MASTER-SKALA-KLINIK.md). **Skala Penyakit ✅ (2026-06-15)** — kategori **Penyakit** (kode `SP-NNNN`) reuse model `SkalaInstrument` via **service factory generik `makeSkalaService`** (DRY: Risiko & Penyakit beda config); `/api/v1/master/skala-penyakit` CRUD + page [/ehis-master/skala-penyakit](src/components/master/skala-penyakit/SkalaPenyakitPage.tsx) (SSR-hybrid, accent violet, kode Auto, DiscardDialog). **Seed 6 klasifikasi** `SP-0001..0006` (Killip/NYHA/TIMI/ECOG/Grade/Stadium AJCC, tervalidasi) via [seed-skala-penyakit.mts](prisma/scripts/seed-skala-penyakit.mts). **Konsumen klinis Jantung/Kanker ✅** — tab Penilaian sub-menu Jantung ([JantungPanel](src/components/igd/tabs/penilaian/JantungPanel.tsx)) & Kanker ([KankerPanel](src/components/igd/tabs/penilaian/KankerPanel.tsx)) = **komposit**: klasifikasi dikomposisi dari master (`GET /master/skala-tersedia?kategori=Penyakit&modul=`, name-match cardiac/onco) + `<ScaleField>`/`computeScale` (shared), narasi/vocab bespoke, **TNM tetap vocab** (bukan skala). Snapshot → **`medicalrecord.PenilaianKomposit`** (1 tabel diskriminasi `jenis`, `data` JSONB, append-only, `/kunjungan/:id/penilaian-komposit?jenis=`). Spec → [docs/BACKEND-MASTER-SKALA-KLINIK.md §B.3-B.4](docs/BACKEND-MASTER-SKALA-KLINIK.md). **Asesmen Katalog ✅ (2026-06-17)** — library referensi dropdown asesmen klinis; model `master.AsesmenItem` (`asesmen_item`) + counter `master.AsesmenCounter` (scope = prefix per kategori) — **11 kategori** (allergen Obat/Makanan/Lainnya + ReaksiAlergi · PenyakitDahulu/Beresiko/Keluarga · PerilakuBeresiko · AnggotaKeluarga · MetodeKB · JenisPersalinan); **kode AUTO-GEN `<PREFIX>-NNN` per kategori** (ALG-OB/ALG-MK/ALG-LN/RX/PD/PB/PK/PR/AK/KB/JP, counter atomik pola SkalaCounter); `/api/v1/master/asesmen-katalog` CRUD layered (leaf tanpa version, gate `master.katalog`) WIRED SSR-hybrid ke [AsesmenKatalogPage](src/components/master/asesmen-katalog/AsesmenKatalogPage.tsx) (kode read-only "Auto"; **list dikelompokkan per kategori + collapsible** dengan keterangan "Dipakai di" sub-tab konsumen; filter sidebar dikelompokkan per sub-tab AllergyPane/RiwayatPane; batal/pindah saat dirty → **DiscardDialog** global, bukan window.confirm). **Seeded 117 item** dari `asesmenKatalogMock` via [seed-asesmen-katalog.mts](prisma/scripts/seed-asesmen-katalog.mts) (counter di-set ke seq tertinggi per prefix). **Konsumen klinis IGD ✅ (2026-06-17)** — `GET /api/v1/master/asesmen-tersedia` (gate `clinical.rekammedis:read`, `scopeKunjungan:false`, reuse service.list status Aktif) + **context fetch-once** [asesmenKatalogContext](src/components/shared/asesmen/asesmenKatalogContext.tsx) (`AsesmenKatalogProvider`/`useAsesmenKatalog`) menyuplai opsi dropdown **AsesmenMedisTab IGD** dari DB — PenyakitDahuluPane · FaktorResikoPane (penyakit/perilaku) · PenyakitKeluargaPane (anggota+penyakit, reconcile saat katalog termuat) · ObstetriPane (KB+jenis persalinan) · AllergyPane (quick-picks per kategori + reaksi + SNOMED). Konstanta `asesmenShared.ts` jadi **fallback** (offline/gagal/sebelum fetch, tanpa kedip). Panel **shared RI/RJ** (shared/asesmen) belum di-wire = follow-up. Spec → [docs/BACKEND-MASTER-KATALOG-KLINIS.md](docs/BACKEND-MASTER-KATALOG-KLINIS.md). **Katalog Radiologi ✅ (2026-06-17)** — `/api/v1/master/rad-catalog` (CRUD layered, **leaf tanpa version**, blok tat/persiapan/kontras/DRL/reporting = **JSONB**, **kode `RAD-NNNN` auto** via `master.RadCatalogCounter`) WIRED SSR-hybrid ke [KatalogRadiologiPage](src/components/master/katalog-radiologi/KatalogRadiologiPage.tsx) (DiscardDialog). **Form: kode read-only "Auto" · Kode ICD-9 OPSIONAL** (manual/dropdown async master ICD via `IcdPicker` `GET /master/icd?jenis=ICD-9`) · **modalitas = method FHIR SatuSehat** (XR/CT/MR/RF/US/MG/DXA/NM + `modalitasSubtype` mis. `CT.angio`) gantikan label lama. **`RAD_KATALOG_MOCK` dihapus** → tipe tetap di `radCatalogMock.ts`, data pindah ke [radCatalogSeed.ts](src/lib/master/radCatalogSeed.ts); **seeded 24 pemeriksaan** (8 modalitas) via [seed-rad-catalog.mts](prisma/scripts/seed-rad-catalog.mts) (counter[RAD]=24). Konsumen `OrderRadTab` + Beranda **dimigrasi off mock** (Beranda baca `RAD_CATALOG_SEED`; **OrderRadTab kini DB-driven via `GET /master/rad-catalog-tersedia` ✅ 2026-06-22** — pemeriksaan ter-assign ruangan Radiologi + harga Tarif Matrix, gate `clinical.tindakan:read`, mirror lab-test-tersedia). Spec → [docs/BACKEND-MASTER-KATALOG-KLINIS.md §C.4](docs/BACKEND-MASTER-KATALOG-KLINIS.md). **Rad → Layanan Unit ✅ (2026-06-17)** — Katalog Radiologi dipetakan ke Ruangan di **Mapping Hub → Layanan Unit** sebagai grup baris **"Tindakan Radiologi"** (matriks terpadu Tindakan+Lab+Rad via `LayananRow` kind `rad`); tabel **paralel** `master.LayananUnitRad` (RadCatalog⇄Location, join hard-delete, idempoten) + `/api/v1/master/layanan-unit-rad` (layered, RBAC `master.mapping`), SSR-hybrid; persist per-kind. **Setiap grup kategori kini collapsible** (chevron header, default terbuka di Layanan Unit, tertutup di mobile) di matrix desktop + mobile. Spec → [docs/BACKEND-MAPPING.md §2](docs/BACKEND-MAPPING.md). **Rad → Tarif Matrix ✅ (2026-06-17)** — Katalog Radiologi juga jadi grup baris **"Tindakan Radiologi"** di **Mapping Hub → Tarif Matrix** (federasi Tindakan+Lab+Rad via `LayananRow` + `rowsFromRad`; `TarifMap` di-key by rowId; edge 3 endpoint dinormalisasi `TarifEdgeLike`); tabel **paralel** `master.TarifRadCatalog` (RadCatalog × penjaminKode × jenisRuangan → harga, upsert-by-triple, FK Restrict) + `/api/v1/master/tarif-rad-catalog` (+`/:id`, gate **`master.tarif`**), SSR-hybrid, dispatch persist per-kind. **Grup kategori collapsible** (chevron header, default **terbuka**) di Tarif Matrix. Spec → [docs/BACKEND-MAPPING.md §5](docs/BACKEND-MAPPING.md). **Komponen Tarif (PMK 85) ✅ (2026-06-18)** — `harga` total dipecah jadi 3 kolom nullable **`jasaSarana` + `jasaMedis` + `jasaParamedis`** (remunerasi) di ke-3 tabel tarif (Tindakan/Lab/Rad; migrasi `20260618120000`). Helper bersama `resolveKomponen`: komponen diisi → `harga = jumlah komponen` (anti-drift); absen → mode total-only (komponen null). Editor sel = **popover 2-mode** (Total saja / Rinci komponen, total auto-sum); bulk-adjust % skalakan komponen proporsional. **Administrasi** = charge level-kunjungan terpisah (tidak di tarif tindakan). **Tarif Ruang Rawat + Administrasi ✅ (2026-07-19)** — pane **Tarif jadi 3 sub-tab segmented** ([TarifHubPane](src/components/master/mapping/tarif/TarifHubPane.tsx), pola Ketersediaan Farmasi): **Matriks Layanan** (eksisting) · **Ruang Rawat** (`master.TarifKamar` Kelas×Penjamin/hari) · **Administrasi** (`master.TarifAdministrasi` Unit×Penjamin). 2 tabel baru (upsert by pair + komponen PMK 85, migrasi `20260719120000`, gate `master.tarif`) + backend layered + client + [TarifSimplePane](src/components/master/mapping/tarif/simpleTarif/TarifSimplePane.tsx) generik (penjamin=tab UMUM aktif, editor inline Total/Rinci, auto-save). **Seed 7 kelas** (dari `AKOMODASI_RATE`) + **3 unit** admin (UMUM). **+Metadata SK opsional** (kolom `noSk`/`tglSk` Date, migrasi `20260719130000`): form Nomor SK + Tanggal SK (DatePicker) → badge indigo di bawah label kelas/unit (tgl disimpan UTC-midnight, aman timezone); hapus tarif = **ConfirmDialog**. **Proyeksi WIRED ✅ (2026-07-19, tutup gap #5 & #1):** `billingProjectionService` baca master — akomodasi = `resolveKamarRate` (kelas×penjamin/hari, fallback konstanta `AKOMODASI_RATE`→0) + charge **Administrasi** per kunjungan `resolveAdminFee` (unit×penjamin, kategori BARU `Administrasi` di `BillingKategori`/`KategoriCharge`/`TotalTagihanWidget`); `penjaminTipe`→`penjaminKode` (BPJS else UMUM). Tarif yang di-set di sini **langsung memengaruhi tagihan** (proyeksi read-only). Verified: proyeksi ambil nilai master ter-edit (bukan konstanta). Spec [docs/BACKEND-MAPPING.md §5b/5c](docs/BACKEND-MAPPING.md). **Seed harga Lab+Rad ✅ (2026-06-18)** — [seed-tarif-lab-rad.mts](prisma/scripts/seed-tarif-lab-rad.mts): harga Umum riset RS tipe C/B (38 tes lab + 24 pemeriksaan rad), **flat lintas 6 tier** (derive dari Location aktif), penjamin UMUM, dengan rincian komponen (Lab 65/10/25 · Rad 55/30/15), upsert by-triple idempoten → **228 baris `tarif_lab_test` + 144 baris `tarif_rad_catalog`**. Spec → [docs/BACKEND-MAPPING.md §5](docs/BACKEND-MAPPING.md). **Konsumen klinis Order Lab ✅ (2026-06-21)** — `GET /api/v1/master/lab-test-tersedia` (read tes lab ter-assign ke ruangan **Laboratorium** via LayananUnitLab, gate **`clinical.tindakan:read`** bukan master, distinct + `ruanganKodes[]` + **harga dari Tarif Matrix** per `penjaminKode`+`jenisRuangan`; layered DAL `listAssignedLabTest`→Service `listLabTestTersedia`→route `scopeKunjungan:false`) dipakai **tab Order Lab shared** (IGD/RI/RJ): katalog DB (Panduan dihapus), **Paket Cepat** di-resolve dari master by kode + total harga, estimasi biaya live. **Template Anamnesis ✅ (2026-06-30, TE2)** — `master.TemplateAnamnesis` (leaf tanpa kode/counter, `contextTags text[]`, migrasi `20260630120000`) CRUD layered gate **`master.konfigurasi`** WIRED SSR-hybrid ke [TemplateAnamnesisPage](src/components/master/template-anamnesis/TemplateAnamnesisPage.tsx); **`TEMPLATE_ANAMNESIS_MOCK` dihapus** → data ke [templateAnamnesisSeed.ts](src/lib/master/templateAnamnesisSeed.ts) (**seeded 17 template** IGD/RI/RJ), tipe+config tetap di `templateAnamnesisMock.ts`. **Konsumen klinis ✅** — `GET /api/v1/master/template-anamnesis-tersedia?modul=` (gate `clinical.rekammedis:read`, `scopeKunjungan:false`) dipakai shared [AnamnesisTemplatePicker](src/components/shared/medical-records/AnamnesisTemplatePicker.tsx) (fetch-by-modul) di **3 pane Anamnesis** IGD/RI/RJ → **3 array hardcode lama dihapus** (DB single source). Spec → [docs/BACKEND-MASTER-TEMPLATE&ENUM.md §3](docs/BACKEND-MASTER-TEMPLATE&ENUM.md). |
| `/ehis-registration`       | Registration     | 🚧 PatientDashboard + KunjunganDetail ✅ · **Backend RJ ✅ (2026-06-04)** — Pasien/Kunjungan/SEP API (layered) + lifecycle worklist + Jaminan persist + noRM `YYMMNNNN` · **IGD/RI Pendaftaran + Bed Allocation ✅ (2026-06-06)** — register IGD (triase opsional+DPJP+ruangan) & RI (kelas+bed reserve); `encounter.BedAllocation` (Reserved/Occupied/Released · partial-unique anti-double-booking · `tersedia`=derived count); IGD occupy-saat-Terima + RI reserve-saat-daftar; board IGD fetch order DB + Terima(pilih bed)/Batalkan + panel ruangan master + okupansi; board loket belum · **Pasien Detail redesign SaaS + Riwayat journey-tree ✅ (2026-06-25)** — hero full-width (KPI tiles, "Buka Rekam Medis" per node), Riwayat = pohon perjalanan gabungan (DPJP+ruangan diresolusi dari master, keterangan SPRI, ruangan di sebelah unit) · **Admisi RI: Bed-Map visual + DPJP ter-assign + Peringatan SPRI ✅ (2026-06-25)** — reservasi bed via peta fullscreen (hijau/merah+nama pemakai/abu, status "Direservasi"; `GET /bed-allocations` di-enrich okupansi pasien), DPJP RI = search dropdown dokter ter-assign ruangan (persist `dpjpId`), banner peringatan bila DPJP ≠ SPRI · **Insert SEP — konektor MOCK server-side ✅ (2026-06-26)** — penerbitan SEP saat Pendaftaran Kunjungan Baru kini lewat **konektor V-Claim mock** (seam swap `callBpjs`): kontrak [schemas/bpjs/sepInsert.ts](src/lib/schemas/bpjs/sepInsert.ts) (`buildInsertSepPayload`→t_sep · `toSepWire`) + [services/bpjs/sepInsertMock.ts](src/lib/services/bpjs/sepInsertMock.ts) (validasi R1–R6 → **error metaData ala asli** kode 201/204/412/500; sukses → noSep via sequence DB); `bpjsService.issueSep` jadi **Result** (`{ok,sep}`/`{ok:false,error}`). **Diagnosa awal SEP RI = diagnosa utama IGD** (SpriDTO di-enrich `diagAwalKode/Nama` via `diagnosaDal.listUtamaByKunjunganIds`, FE prefill saat pilih SPRI). **No. Telp = `Pasien.noHp`** (prefill + peringatan wajib). **Penanganan error step Review:** SEP ditolak → modal tetap terbuka, **alihkan ke step SEP** + banner 2 pilihan **"Tetap Daftarkan (SEP ditangguhkan, `forceSep`)"** / **"Revisi Dulu"** (`KunjunganDTO.sepError` transient · default = rollback). Doc [docs/MOCK-SEP-INSERT.md](docs/MOCK-SEP-INSERT.md). **`skdp.kodeDPJP` ter-resolve ✅** — rantai `No.Ref SPRI → SPRI.dpjpPegawaiId → mapping Dokter↔kodeDpjpBpjs` (fallback DPJP kunjungan), `""` bila belum di-map di Mapping Hub DPJP BPJS. Gap sisa: `dpjpLayan` (RJ) masih teks form · `lokasiLaka` laka · `noRujukan` internal auto-gen · **Worklist Admisi RI (SPRI) ✅ (2026-06-29)** — [AdmisiRanapBoard](src/components/registration/admisi/AdmisiRanapBoard.tsx) (Beranda tab "Admisi IGD & RI"): kartu SPRI belum-dikonsumsi + aksi **Daftar Rawat Inap** · **Revisi SPRI** ([SpriEditModal](src/components/registration/admisi/SpriEditModal.tsx) → RencanaKontrol/UpdateSPRI BPJS) · **Batalkan SPRI** (RencanaKontrol/Delete BPJS) · **Kirim Ulang** (retry No.Ref); picker pakai shared `DatePicker`/`Select` (modal z-50 < popover z-60). Lifecycle SPRI lengkap (konsumsi→**auto-tutup saudara `Tergantikan`** · **pulih saat batal order RI** · edit/cancel guard `Dikonsumsi`) didokumentasikan di baris **Rawat Inap** · **Buat SPRI mandiri di Admisi ✅ (2026-07-11)** — tombol **"Buat SPRI Baru"** di [AdmisiRanapBoard](src/components/registration/admisi/AdmisiRanapBoard.tsx) → [SpriCreateModal](src/components/registration/admisi/SpriCreateModal.tsx) untuk rawat inap **terencana** (bukan dari IGD), **form penerbitan = komponen [SPRIPanel](src/components/igd/tabs/pasienPulang/SPRIPanel.tsx) yang SAMA PERSIS dgn IGD** (bukan hand-rolled → tampilan identik): modal hanya menambah pilih pasien (`searchPatients` nama/RM) + **kunjungan sumber** (`listKunjungan?patientId`, default terbaru — tautan provenance + diagAwal SEP RI); SPRIPanel dapat `roster` dari `listDokter` (spesialistik via jembatan `spesialistikFromSpesialisKode` di smfPoliMap → SMF/poli auto-derive `resolvePoliBpjs` bekerja spt IGD) + prop baru opsional `submitHint` (teks penutup "Terbitkan SPRI"). Submit → `POST /api/v1/spri` (**BARU**, gate `registration.kunjungan:create`, `scopeKunjungan:false`) → `spriService.create`: No. Kartu **di-resolusi server** dari penjamin BPJS pasien (`decryptPii`, anti-spoof; FE kirim kosong) + No. Referensi terbit server (mock `issueSpriRef`, status `Terbit`/`MenungguRef`) + `pencatat`/`user` = actor login → kartu langsung muncul di worklist (refetch). **Tanpa schema baru** (reuse `encounter.Spri` + `spriDal.create`); layered Zod `CreateSpriInput`/route/service/`createSpri` client |
| `/ehis-dashboard`          | Dashboard        | 🔧 Scaffold (belum dibangun)    |
| `/ehis-billing`            | Billing Kasir    | ✅ **Core 100% operasional** — BL1 Tagihan Board + BL2 Invoice Detail 4-tab + BL3 Kasir Counter 3-tab + **BL8 Beranda Billing** (KPI Strip + Quick Nav + 3 panel) + **BL6 ~80%** Charge Ingestion reactive `useSyncExternalStore` (Lab/Rad/Farmasi/Akomodasi silent-wired; Discharge Banner RI; Mini Widget RI) + Single-source refactor (registrasi read-only + deep-link). **Sisa:** BL5 Adjustment · BL7 Reports · BL9 Polish · BL6 Tindakan+JasaDokter triggers · BL6 Mini Widget IGD/RJ. Roadmap [TODO-BILLING.md](TODO-BILLING.md). **Backend link ke order NYATA — Slice 1 proyeksi read-only ✅ (2026-07-13):** charge = **PROYEKSI order** (bukan duplikasi; harga snapshot) via `GET /kunjungan/:id/billing` ([billingProjectionService](src/components/../lib/services/billing/billingProjectionService.ts), gate `billing.invoice:read` lintas-unit) → 5 tabel order + akomodasi RI (basis `kelasHak`); rincian read-only di `/ehis-billing/tagihan/kunjungan/[kid]`. **2 pintu masuk:** deep-link chip Total Tagihan header rekam medis + **tab Tagihan** `/ehis-billing/tagihan` (tanpa tab baru & tampilan board asli DIPERTAHANKAN — [TagihanBoardPage](src/components/billing/tagihan/TagihanBoardPage.tsx) Hero+KPI+Filter+Table tetap, hanya data di-swap mock→proyeksi via `GET /billing/kunjungan` + adapter [mapProjectionRow](src/components/billing/tagihan/realRows.ts)→`TagihanRow`; komponen jadi prop-driven `rows`; baris → rincian `[kid]`). **Slice 2a fondasi persist ✅ (2026-07-14):** schema Postgres ke-8 **`billing`** — `Invoice` (1/kunjungan lazy) + `Payment` (void bukan delete, `kasir` server-resolved) + `BillingCounter` (INV/KW); charge tetap PROYEKSI, total/sisa/status DIHITUNG. Service [billingInvoiceService](src/lib/services/billing/billingInvoiceService.ts) (`getInvoiceState`/`recordPayment`/`voidPayment`) + endpoint `/kunjungan/:id/billing/{invoice,payment}` (gate `billing.invoice:read` / `billing.kasir:create`). **Kasir = SATU pintu bayar** (keputusan). **Slice 2b+2c ✅ (2026-07-14):** **board Tagihan status/dibayar/sisa NYATA** — `listKunjunganBilling` di-join invoice+payment (`billingReadDal.aggregatePaid` `= ANY(uuid[])`, non-void) → `billingStatus` (Draft/Belum Lunas/Lunas Sebagian/Lunas) via helper bersama [billingStatus.ts](src/lib/services/billing/billingStatus.ts) (dipakai detail & worklist, anti-drift) → [mapProjectionRow](src/components/billing/tagihan/realRows.ts) pakai `dibayar`+status nyata. **Detail tagihan adopsi DESAIN InvoiceDetailPage** (banner + 4 tab) tapi DATA NYATA + READ-ONLY: [KunjunganInvoiceDetail](src/components/billing/invoice/KunjunganInvoiceDetail.tsx) (`GET /kunjungan/:id/billing/invoice` `getInvoiceState` → map `InvoiceStateDTO`→`InvoiceDetail`; reaktif `recordBus "order"`) — **Rincian** = charge proyeksi read-only (prop `readOnly` di-thread ke RincianChargeTab/ChargeCategorySection/ChargeRow/ChargeStickyFooter: sembunyikan tambah/diskon/void/finalize) · **Pembayaran** [KunjunganPembayaranReadonly](src/components/billing/invoice/tabs/KunjunganPembayaranReadonly.tsx) = read-only (summary + riwayat) + CTA **"Terima Pembayaran"** deep-link Kasir (satu pintu) · **Klaim/Audit** reuse read-only. `InvoiceStateDTO` di-enrich pasien gender/age + waktuKunjungan. **Kasir Quick Bayar 100% NYATA (mock dihapus, 2026-07-14):** search outstanding = proyeksi billing (`GET /billing/kunjungan`→`mapProjectionRow`, sisa dari DB) · bayar = `recordPayment` (kwitansi KW, kasir server-resolved) · **recent feed = `GET /billing/payments/recent?shiftId=&limit=`** (endpoint BARU, non-void, join billing.*+header kunjungan) · ChargeSummaryCard UUID-aware (fetch `getInvoiceState`→`buildChargeSummary`) · deep-link `?tab=quick&invoice=<kid>` **pre-select** tagihan ke form yang SAMA (desain tak berubah; `KasirInvoicePayPanel` dihapus, unified). `outstandingSearch`/`chargeSummary` di-refactor terima source nyata; mapper `invoiceStateToDetail` diekstrak ke [invoiceStateMap](src/components/billing/invoice/invoiceStateMap.ts) (dipakai detail + kasir). **Dashboard Kasir de-mock (2026-07-14):** angka Dashboard 100% dari pembayaran NYATA — endpoint BARU `GET /billing/payments/summary?shiftId=&date=` (agregat `byMetode`/`totalTransaksi`/`totalRefund`, non-void, `billingReadDal.aggregatePaymentSummary`); [KasirCounterPage](src/components/billing/kasir/KasirCounterPage.tsx) mulai **tanpa shift** (KASIR_SHIFT_MOCK dibuang dari page → EmptyShiftState), shift aktif **di-hidrasi** total dari summary by shiftId, KPI hari ini dari summary by date ([ShiftKPIStrip](src/components/billing/kasir/ShiftKPIStrip.tsx) terima prop `agg` + caption "Belum ada transaksi hari ini"); accumulator client dihapus (total di-derive dari `billing.payment`), tutup shift snapshot total nyata; empty state RecentShiftsTable/EmptyShiftState eksisting. Shift = sesi kasir client (buka/tutup belum persist → Recent reset saat reload; `KASIR_SHIFT_MOCK` masih dipakai Beranda BL8 + setoran modal). **Deposit Awal de-mock (2026-07-17):** tab Deposit Awal 100% NYATA (desain tetap) — `PASIEN_ADMISI_MOCK` (+`removePasienAdmisi`) DIHAPUS; daftar admisi pending = **kunjungan Rawat Inap belum ada pembayaran** dari proyeksi billing (`GET /billing/kunjungan`) via adapter [realAdmisi](src/components/billing/kasir/deposit/realAdmisi.ts) (`isDepositPending` unit=RawatInap & `dibayar<=0` → `toPendingAdmisi` map→`PasienAdmisi`; kelas/penjamin dipetakan, kategori/urgensi/DPJP default — AdmisiResultRow sembunyikan DPJP kosong); [DepositAwalPanel](src/components/billing/kasir/deposit/DepositAwalPanel.tsx) jadi **prop-driven** (`pending`/`loading` di-fetch page-level, badge tab = jumlah nyata), submit = **`recordPayment` kategori "Deposit"** (invoice lazy-create + kwitansi dari state nyata `invoiceStateToDetail`, kasir server-resolved) → pasien lepas dari daftar sesudah refetch; `suggestDeposit`/`EstimateChargeCard`/`DraftInvoicePreview` tetap (heuristik saran, bukan data). Empty-state "belum ada pasien RI menunggu deposit". Batas: worklist billing **order-driven** → RI admisi tanpa order (akomodasi saja) belum muncul (follow-up: perluas worklist ke RI order-less). **Shift kasir PERSIST ✅ (2026-07-17, Slice 2e):** shift buka/tutup kini **tersimpan DB** (schema `billing` tabel BARU **`Shift`** — migrasi `20260717120000`; owner sesi=`authorUserId`, operator=`kasirPegawaiId`, totals Open=proyeksi payment · Closed=snapshot JSONB saat tutup) → **sesi kasir BERTAHAN lintas navigasi** (bug "buka kasir lagi tiap pindah halaman" tuntas). Layered: `shiftDal`/`shiftService` (buka: cek counter tak double-open + resolve operator dari user role "Kasir" anti-spoof · tutup: snapshot totals dari `aggregatePaymentByShifts` + hitung selisih server) + endpoint `GET/POST /billing/shift` · `PATCH /billing/shift/:id/tutup` · `GET /billing/kasir-users` (gate `billing.kasir` read/create) + client [api/billing/shift](src/lib/api/billing/shift.ts). [KasirCounterPage](src/components/billing/kasir/KasirCounterPage.tsx) fetch **papan** `{active (shift saya), open (occupancy+counter lain, totals live), recentClosed (snapshot)}` tiap mount+mutasi; buka/tutup → API → refetch. **Dropdown Kasil di [BukaShiftModal](src/components/billing/kasir/modals/BukaShiftModal.tsx) = NYATA** — `GET /billing/kasir-users` (user role Kasir + `unitKerja` ILIKE kasir) via komponen **`Select` portal** (bukan `<select>` native lagi); `KASIR_LIST` mock tak dipakai. Sisa mock Kasir: **setoran** belum persist (cetak slip saja). **Beranda Billing (BL8) de-mock ✅ (2026-07-17):** landing `/ehis-billing` 100% NYATA (desain tetap; Card grup **Laporan** & tombol disable dibiarkan) — [BerandaBillingPage](src/components/billing/beranda/BerandaBillingPage.tsx) fetch paralel `listBillingKunjungan` + `getPaymentSummary({date})` + `getShiftBoard` + `listRecentPayments` → agregator murni [berandaBillingShared](src/components/billing/beranda/berandaBillingShared.ts) (`getBillingStats(input)` menerima rows/pendapatan/openShifts, bukan baca mock) + `getPasienSiapBayar(rows)`; **`TAGIHAN_BOARD_MOCK`/`KASIR_SHIFT_MOCK`/`SHIFT_PAYMENTS_MOCK`/`KLAIM_HARI_INI_MOCK`/`getRecentPaymentsLintasCounter`/`TODAY_ISO` DIHAPUS** dari beranda. 5 KPI NYATA (Tagihan Hari Ini · Outstanding · **Klaim Pending = outstanding penjamin non-Umum** · Pendapatan Hari Ini `totalMasuk−totalRefund` · Shift Aktif); panel [PasienSiapBayarPanel](src/components/billing/beranda/PasienSiapBayarPanel.tsx)/[RecentPaymentsPanel](src/components/billing/beranda/RecentPaymentsPanel.tsx)/[KlaimHariIniPanel](src/components/billing/beranda/KlaimHariIniPanel.tsx) jadi **prop-driven** (deep-link → `/tagihan/kunjungan/:kid`; recent feed counter→kasir; Klaim = `entries=[]` empty-state karena E-Klaim belum ada backend); render digate `loaded && dataReady`, empty-state eksisting dipertahankan. **Penyesuaian + Refund + Void — Slice 2d Fase 1 ✅ (2026-07-17):** **Adjustment level-invoice** (diskon/materai/PPN) — kolom `Invoice.{diskonInvoice,materai,ppnPct}` sudah ada (math di `getInvoiceState`: `grand=(subtotal−diskon)+PPN+materai`); endpoint BARU `PATCH /kunjungan/:id/billing/invoice/adjustment` (gate `billing.invoice:update`, `invoiceDal.updateAdjustment` guard versi optimistic, `setAdjustment` lazy-create+validasi diskon≤subtotal) + client `setInvoiceAdjustment`; UI: [AdjustmentModal](src/components/billing/invoice/modals/AdjustmentModal.tsx) (diskon Rp + hint %, materai, PPN%, alasan, preview grand live) dibuka via tombol **"Penyesuaian"** di [ChargeStickyFooter](src/components/billing/invoice/tabs/ChargeStickyFooter.tsx) (prop BARU `onAdjust` — muncul di mode read-only TANPA membuka charge rows) di [KunjunganInvoiceDetail](src/components/billing/invoice/KunjunganInvoiceDetail.tsx), digate `useCan("billing.invoice","update")`. **Void kwitansi INLINE di detail** (tab Pembayaran [KunjunganPembayaranReadonly](src/components/billing/invoice/tabs/KunjunganPembayaranReadonly.tsx)) — tombol **Batalkan** per baris (gate `billing.kasir:create`, dialog alasan wajib → `voidPayment` guarded, `voidedBy` server; `onChanged`→refetch); banner diperbarui (refund via Kasir, void di sini). **Refund via Kasir** (terikat shift) — deep-link `?tab=quick&invoice=<kid>&mode=refund` dari detail (tombol **Refund** saat `dibayar>0`) → [QuickPaymentForm](src/components/billing/kasir/quick/QuickPaymentForm.tsx) prop `mode="refund"` (kategori Refund, batas=`dibayar`, accent rose, "Proses Refund") → [QuickBayarPanel](src/components/billing/kasir/quick/QuickBayarPanel.tsx) `recordPayment` **kategori=`payload.kategori`** (bukan hardcode; refund nominal di-sign negatif server); `deepLinkMode` di-thread page→KasirCounterPage→panel. **Fase 2 — diskon/void per ITEM ✅ (2026-07-21):** tabel BARU `billing.ItemAdjustment` (overlay, `invoiceId`+`sourceRef` unique, `reduksi` Rp snapshot; migrasi `20260721120000`) — diskon (Rp/pct) & void per baris charge (proyeksi read-only) via overlay `applyItemAdjustments` di `buildState` (Draft & Final, blokir tambah saat Final); board konsisten via Σ`reduksi`; audit `item.diskon`/`item.diskon.hapus`/`item.void`/`item.unvoid` (tiap aksi & pembatalannya punya action sendiri, tone diskon = amber); `POST`/`DELETE /kunjungan/:id/billing/item-adjustment`; FE kebab `ChargeRow` di-un-gate (`allowItemAdjust`, Draft only) + [ItemAdjustModal](src/components/billing/invoice/modals/ItemAdjustModal.tsx). **P2 — RI discharge gate/widget de-mock ✅ (2026-07-19):** gate pulang RI + chip sisa header **berhenti baca `billingStore` mock**, kini pakai billing NYATA via endpoint klinis BARU `GET /kunjungan/:id/billing/ringkas` ([billingInvoiceService.getRingkas](src/lib/services/billing/billingInvoiceService.ts) = slim `getInvoiceState`: status+subtotal+grandTotal+dibayar+sisa+penjaminTipe) **gate `clinical.rekammedis:read`** (bukan `billing.invoice` — Dokter/Perawat boleh lihat sisa pasiennya; ABAC careUnit via scopeKunjungan) + client `getBillingRingkas`. Hook bersama [useBillingRingkas](src/components/shared/medical-records/useBillingRingkas.ts) (UUID-guard + reaktif `recordBus "order"`); [BillingMiniWidget](src/components/shared/medical-records/BillingMiniWidget.tsx) + [BillingGateBanner](src/components/rawat-inap/pasienPulang/BillingGateBanner.tsx) di-rewrite prop **`kunjunganId`** (was `noRM`), deep-link → `/tagihan/kunjungan/[kid]` (bukan route mock `[id]`); gate penjamin-aware (Umum outstanding=rose · BPJS/Asuransi=sky klaim · lunas=emerald · demo/subtotal 0=slate). **Ingest client MATI** — `ingestLabOrder/ingestRadOrder/ingestFarmasiOrder` dicabut dari Lab/Rad(2)/Farmasi pane (charge=PROYEKSI server, tulisan ke mock store = dead). **Retire mock lama ✅ (2026-07-21):** route `/tagihan/[id]` + `InvoiceDetailPage` + `billingStore`/`chargeIngest`/`sourceAdapter`/`priceResolver` **DIHAPUS**; 4 deep-link dipindah ke `/tagihan/kunjungan/[kid]` (Kasir ChargeSummaryCard · E-Klaim ClaimBannerHeader+berkasShared via `claim.kunjunganId` · registration BillingDetailModal → board). Ikut mati & dihapus: `obatPriceCatalog`+`ObatPriceHydrator` + **SSR-prefetch 300 baris `master.obat` di layout billing**, serta `AUDIT_EVENTS_MOCK` (~208 baris event fiktif) di `auditTrail` (prop `events` RiwayatAuditTab jadi wajib). `invoiceMock`/`chargeSummary`/`kwitansiContext`/`tagihanBoardMock` masih hidup utk jalur Kasir deposit/kwitansi. **Finalize / lock lifecycle — Slice 2f ✅ (2026-07-19):** charge kini bisa **DIBEKUKAN** di billing (bukan saat discharge — charge sering menetes pasca-pulang). Kolom `billing.Invoice.{status,finalizedAt,finalizedBy}` + tabel BARU **`billing.InvoiceItem`** snapshot baris (migrasi `20260719140000`). **Draft** = proyeksi HIDUP · **Final** = snapshot BEKU (`buildState` bercabang). `finalizeInvoice(force?)` proyeksi→InvoiceItem 1 tx (guard `setFinal` WHERE status='Draft'+versi; untariffed>0 ditolak kecuali `force`=peringatan) · `reopenInvoice(alasan)` buang snapshot→Draft (pembayaran tetap); adjustment diblokir saat Final. Gate **`billing.invoice:update`**, route `POST /kunjungan/:id/billing/{finalize,reopen}`. FE [InvoiceFinalizeBar](src/components/billing/invoice/InvoiceFinalizeBar.tsx) (strip Draft/Final + modal konfirmasi/reopen) di [KunjunganInvoiceDetail](src/components/billing/invoice/KunjunganInvoiceDetail.tsx); `lifecycle` di `InvoiceStateDTO`/`BillingRingkasDTO`. Prasyarat Reports (agregasi `InvoiceItem` per periode). **Audit Trail — Slice 2g ✅ (2026-07-20):** tabel BARU **`billing.AuditLog`** (soft-ref invoice+kunjungan, `meta` JSONB diff/target; migrasi `20260720120000`) — jejak IMMUTABLE tiap mutasi finansial ditulis dalam tx aksi: `invoice.create`/`finalize`/**`reopen`**/`diskon` + `payment.add`/`refund`/`void` (aktor server-resolved anti-spoof). `GET /kunjungan/:id/billing/audit` (gate `billing.invoice:read`) → tab **Riwayat Audit** kini DATA NYATA ([RiwayatAuditTab](src/components/billing/invoice/tabs/RiwayatAuditTab.tsx) prop `events`; mock hanya fallback route lama). Menutup gap "reopen who/why" Slice 2f. **Auto-finalisasi saat pelunasan + konfirmasi bayar ✅ (2026-07-20):** `recordPayment` kini **auto-finalisasi** bila pembayaran kategori "Pembayaran" membuat **sisa = 0** (tagihan Draft, subtotal>0; `force` agar untariffed tak memblokir; best-effort — gagal finalisasi tak batalkan bayar). **Deposit/Refund/cicilan TIDAK memicu.** FE [QuickPaymentForm](src/components/billing/kasir/quick/QuickPaymentForm.tsx) tambah **konfirmasi box** sebelum submit + **pemberitahuan kondisional**: lunas → "akan otomatis difinalisasi" (emerald/Lock), cicilan → "sisa RpX, tetap Draft", refund → info refund. |
| `/ehis-eklaim`             | E-Klaim          | ✅ **EK0–EK9 100% SELESAI** (2026-05-30) — Beranda (KPI+Pipeline+ActivityTab) · Klaim Board 11-col+bulk+density · Klaim Detail 6-tab (Berkas/Coding/Grouper/Submission/Audit/Timeline) · iDRG Calculator 3-mode · Berkas Generator A4 (ResumeMedis/BerkasKlaim/SuratPengantar) · Banding Board+Form+Detail · Reconciliation (ImportCSV+MatchingEngine+SelisihWriteOff+DetailPage+PrintTemplate) · Dashboard 5-tab (ApprovalRate/Aging/MarginAnalysis/CoderProductivity/MarginComparator) · EK9 Polish: print stylesheet + animasi stagger + density toggle + cross-modul links (PenjaminDetail/BillingGateBanner/IGD+RI+RJPatientHeader). iDRG-first · TSC clean. Roadmap [TODO-EKLAIM.md](TODO-EKLAIM.md) |
| `/ehis-bpjs`               | BPJS Integration Hub | ✅ **BP0–BP8 100%** — 16 lib [src/lib/bpjs/](src/lib/bpjs/) · BP1 Beranda (KPI+Sidebar 2-tab Live Calls/Referensi) · BP2 Kepesertaan (cek NIK/NoKartu) · BP3 SEP 6-tab (Cari/Hapus/UpdateTgl/Integrasi/Suplesi JR/SEP Internal/Fingerprint) · BP4 Rujukan (Cari+Khusus+Spesialistik+Sarana+Referensi 3-tab) · BP5 Monitoring 4-tab (Kunjungan/Klaim/Histori/Jasa Raharja) · BP6 Rencana Kontrol 7-tab (11 endpoint+PRB 9 penyakit kronik+SPRI) · BP7 Aplicares 3-halaman (ReferensiKamar+MapKelas CRUD+Ketersediaan BedSync) · BP8 Polish (AuditTrail filter+export · RefSync Scheduler · Error Recovery+Toast · **3 Print Template SEP/RK-SPRI/Audit A4**). Cross-link: `/ehis-eklaim` (klaim consume V-Claim) · `/ehis-registration` (SEP saat admisi) · `/ehis-master/ruangan` (Aplicares bed sync). Roadmap [TODO-BPJS.md](TODO-BPJS.md) |
| `/ehis-inventory`          | Inventory (Logistik) | ✅ **FE + Backend (BE0–BE8) 100%** (2026-06-19) — modul BARU (logistik/gudang lintas-katalog Obat+BMHP), 7 menu: Beranda · Daftar Barang · Stok Opname · Pengiriman (Penerimaan+Transfer) · Distribusi · Rekanan · Monitoring. **Schema Postgres ke-7 `inventory`**: **movement-ledger `StockMovement` = source of truth** (append-only immutable) · **saldo = proyeksi** (`StockBatch` per batch+ED FEFO + `StockBalance` rollup) di-update transaksional via primitif tunggal **`movementService.postMovement`** (IN/OUT/TRANSFER/ADJUST/OPNAME, anti-negatif `decBatchGuarded`, FEFO). Item = `itemJenis`+`itemId` soft-ref master (tanpa FK lintas-schema, validasi Service); lokasi = farmasi (Depo/Gudang). **Alur**: Penerimaan create→post IN · Transfer/Distribusi 2-fase (draft **reservasi `qtyReserved`** guard atomik → post TRANSFER + lepas reservasi; cancel lepas) · Opname snapshot→save fisik→post OPNAME per selisih (atau **cancel→Dibatalkan** sebelum posting, tanpa sentuh ledger) · Beranda/Monitoring agregat read (nilai/reorder/ED-FEFO/movers). RBAC `inventory.{view,barang,opname,pengiriman,distribusi,monitoring,rekanan}` (Apoteker). Layered Route→Service→DAL; kode dok auto `GRN/TRF/DST/OPN-<YYMM><NNN>` + `VND-<NNN>` via `InvCounter`. **Distribusi Obat di Mapping Hub dihapus** (digantikan modul ini). Seed dari edge Ketersediaan Farmasi. **Penyempurnaan 2026-06-20**: Stok Opname — pengisian via modal interaktif + paginasi + **Batalkan SO** (status `Dibatalkan` merah, sesi read-only; migrasi enum `20260620120000`) + **Cetak Berita Acara A4** (sesi Posted); Daftar Barang — **Penyesuaian stok** (ADJUST 1 item) + editor kebijakan **ROP/min/max** + paginasi; Penerimaan — **ED opsional** (No. Batch opsional, batch turun dari ED → `ED-YYYYMMDD`, atau pool `UMUM` bila dua-duanya kosong). Roadmap FE [TODO-INVENTORY.md](TODO-INVENTORY.md) · Backend [TODO-INVENTORY-BACKEND.md](TODO-INVENTORY-BACKEND.md). **Follow-up**: Vendor edit/delete UI · partial-fulfill · distribusi→unit klinis (OUT) · integrasi dispensing Farmasi→OUT + billing |
| `/ehis-report`             | Reports          | 🔧 Scaffold (belum dibangun)    |
| `/ehis-fhir`               | FHIR Integration | 📋 Planned (terpisah dari master) |

Shared layout: `Navbar` · `Sidebar` · `ModuleSwitcher` · `ModuleLayout` → `src/components/layout/`

**Detail tab/feature per modul yang ✅ Done:** lihat [.claude/DONE.md](.claude/DONE.md).

---

## 🔴 Active Work / Next Up

Frontend Phase 0–3 master sudah selesai 100%. Workload selanjutnya bisa dipilih dari:

### Backend Integration (rekomendasi utama)
- Mulai dari [TODOS_BACKEND.md](TODOS_BACKEND.md) Phase B0 — Foundation (Prisma + Auth + RBAC + Infra).
- Schema mock sudah 1:1 dengan target — swap `import { X_MOCK }` → `await prisma.x.findMany()` tanpa refactor UI.

### Modul Baru (frontend lanjutan)
- [ ] **`ehis-dashboard`** — stats cards (pasien hari ini per unit IGD/RI/RJ) + BOR chart + recent activity feed + quick-nav ke modul lain.
- [✅] **`ehis-billing`** Kasir — **Core operasional 100%**. **Roadmap [TODO-BILLING.md](TODO-BILLING.md)** (35 task, ~20.5/35 = 59%). BL1 Tagihan Board ✅ + BL2 Invoice Detail 4-tab ✅ + BL3 Kasir Counter 3-tab ✅ + **BL8 Beranda Billing** ✅ + **BL6 Charge Ingestion** ~80% (foundation libs `priceResolver`/`sourceAdapter`/`billingStore`/`chargeIngest` siap · Lab/Rad/Farmasi/Akomodasi silent-wired reactive · Discharge Banner RI · Mini Widget RI breadcrumb · Single-source refactor `/ehis-registration` jadi read-only). **Sisa fase ditunda** (BL5 Adjustment · BL7 Reports · BL9 Polish · BL0 formal types · BL6 Tindakan+JasaDokter triggers · BL6 Mini Widget IGD/RJ) — akan di-pick up sesuai prioritas business.
- [✅] **`ehis-eklaim`** Klaim — **100% SELESAI** (2026-05-30). **Roadmap [TODO-EKLAIM.md](TODO-EKLAIM.md)** (39/39 task, EK0–EK9 ✅). EK0 Foundation · EK1 Beranda V2 · EK2 Klaim Board 11-col+bulk+density · EK3 Klaim Detail 6-tab+3 modal · EK4 iDRG Calculator 3-mode · EK5 Berkas Generator A4 · EK6 Banding Board+Form+Detail · EK7 Reconciliation (Import+Match+WriteOff+DetailPage+PrintTemplate+CSV) · EK8 Dashboard 5-tab (ApprovalRate+Aging+Margin+Coder+Comparator) + CSV/PDF export · EK9 Polish (print stylesheet + animasi + density toggle + cross-modul links).
- [✅] **`ehis-bpjs`** BPJS Integration Hub — **100% SELESAI** (2026-05-30). **Roadmap [TODO-BPJS.md](TODO-BPJS.md)** (44/44 section). BP0–BP8 selesai: Lib 16 file + Beranda + Kepesertaan + SEP 6-tab + Rujukan + Monitoring + Rencana Kontrol + Aplicares BedSync + Audit Trail + 3 Print Template (SEP/RK-SPRI/Audit) + Workflow Docs. Backend integration → lihat [TODOS_BACKEND.md](TODOS_BACKEND.md) Phase B-BPJS.
- [🚧] **`ehis-registration`** — **Backend RJ Integration ✅ (2026-06-04)** [TODO-REGISTRASI.md](TODO-REGISTRASI.md#phase-reg-be--backend-integration-loket--db-2026-06-04): Pasien API (register dedup-first + complete + **updatePenjamin**) · Kunjungan API (register RJ + worklist cursor + detail) · **SEP mock** (terbit+cetak A4, tersimpan DB) · **lifecycle worklist** (check-in/call/recall/receive/complete/cancel, version-guarded) wired ke board RJ · **Jaminan persist** (jaminan ikut kunjungan terakhir, single-primary invariant) · **noRM `YYMMNNNN`** reset/bulan (counter table atomik) · Riwayat+Jaminan dashboard wired. **Pasien Baru** ([PasienBaruModal](src/components/registration/pasien-baru/PasienBaruModal.tsx)) sudah submit ke `POST /patients` (dedup-first). **Detail Kunjungan** (`/pasien/:id/kunjungan/:kid`) fetch DB via `GET /kunjungan/:id` (REG-BE.7 G-A/G-B/G-H, format mock). **IGD/RI Pendaftaran + Bed Allocation ✅ (2026-06-06, REG-BE.8)** — `registerKunjungan` kini terima IGD (triase opsional+DPJP+ruangan dari master) & RI (kelas+bed); model `encounter.BedAllocation` row-based (`tersedia`=derived, partial-unique anti-double-booking 409); IGD occupy-saat-Terima, RI reserve-saat-daftar, release-saat-cancel/complete; board IGD ([IGDWorkspace](src/components/igd/IGDWorkspace.tsx)) fetch order DB belum-diterima + Terima(pilih bed)/Batalkan + panel ruangan master + okupansi + resolve DPJP/ruangan/bed dari master; mock IGD dihapus kecuali Joko (igd-1). **Sisa:** board loket · realtime board (SSE) · nama DPJP riwayat RJ · rekam medis IGD dari DB (kartu pasien DB belum bisa di-link, detail page masih 100% mock) · board admisi RI penuh · dokumen kunjungan (schema) · tab Aksi kunjungan · auto-redirect pasca daftar pasien.
- [ ] **`ehis-report`** — laporan per periode + export Excel/PDF.
- [ ] **`ehis-fhir`** — modul integrasi SatuSehat (kredensial · sync resource · NIK lookup · sync log · conflict resolution).
- [ ] **Master Tier 3 — Poliklinik & Jadwal Dokter** — kapasitas antrian per poli per hari, jadwal buka, weekly schedule grid. Lihat [TECH_DEBT.md](TECH_DEBT.md#master--other).

### Tech Debt Resolution
- Lihat [TECH_DEBT.md](TECH_DEBT.md) untuk daftar lengkap per-modul + cross-cutting.

---

## 🏗 Key Architecture Decisions (jangan diubah tanpa diskusi)

### Master Data
- **Organization & Location UI**: Unified Tree — 1 route `/ehis-master/ruangan`, n-level Organization nested via `parentId`, Bed sebagai sub-collection `LocationNode.beds[]`. (2026-05-19)
- **FHIR Strategy**: SEMUA interaksi FHIR/SatuSehat (sync action, NIK lookup, Org_id config, mapping config) **pindah ke modul terpisah `/ehis-fhir`** (belum dibangun). Master pages = data RS murni. Adapter Pattern (`toFhirOrganization()`/dst) tetap di `lib/fhir/adapters/` saat backend ready. (2026-05-19, revisi dari rencana awal yang embed FHIR di master)
- **Mapping Strategy (Opsi A — Mapping Hub Terpadu)**: Semua relasi N:N antar entitas master di-host di 1 hub `/ehis-master/mapping` dengan sidebar internal. Source of truth tetap di Hub — UI edit penugasan di entitas master **dihapus** (DokterDetail.poliAssignment + PenggunaFormModal.unitAssignment hanya tinggal MappingSourceBadge cross-link). Field tetap di schema sebagai seed default. (2026-05-22)
- **Address**: Convention over Configuration — Location inherit dari parent Organization secara default, override per record via flag.
- **Kode wilayah** (direvisi 2026-06-02): **source of truth = tabel `master.Wilayah` di DB** (di-seed dataset resmi Kemendagri; provinsi→kab/kota→kec→kelurahan, self-ref `parentKode`), diakses via **internal API `/api/v1/wilayah?level=&parentKode=` lazy per level + cache** (cache-aside Redis + TanStack client). Cascading dropdown konsumsi API ini; embed JSON hanya boleh fallback level-atas (first-paint), bukan source-of-truth. **JANGAN hit API wilayah publik internet** (tak ber-SLA, egress jaringan klinis, tak otoritatif, tak joinable). Kode numerik Kemendagri wajib untuk FHIR `administrativeCode`. **Kode BPJS berbeda** → di-sync dari endpoint referensi BPJS (cron/outbox, bukan live) ke reference terpisah + tabel mapping Kemendagri↔BPJS.
- **Bed status operasional** (`Tersedia/Terisi`): dikelola workflow klinis saat admisi/pulang, **bukan** form master.
- **Practitioner master**: data dokter manual input. NIK lookup ke SatuSehat untuk verifikasi/auto-populate **pindah ke modul `/ehis-fhir`**.

### Data Flow
- **`ORDERS_MOCK` = single source of truth** untuk Lab/Rad/Farmasi/Resep. Saat migrasi ke DB, tabel `Order` jadi single source — UI tidak berubah.
- **Workflow store pattern**: state mutasi (telaah farmasi, hasil lab, dosis rad) saat ini di `workflowStore` client-side. Backend perlu commit ke database + push update via WebSocket/SSE atau polling.
- **Mock-first → swap pattern**: semua mock di `src/lib/master/*Mock.ts` punya schema 1:1 dengan target Prisma. Migrasi = ganti import. Zero refactor UI.

### UI
- **Density tokens** (`m-mini/m-tiny/m-xs/m-sm/m-base/m-lg`): utility classes berbasis CSS custom properties di `globals.css`. Mengikuti `data-density` attribute (Compact/Comfortable/Cozy). Toggle di Mapping Hub header.
- **Skeleton 500ms** via `useSkeletonDelay()` untuk semua master pages + farmasi/lab/rad worklist.
- **MappingSourceBadge** (3 variant: card/banner/inline) untuk cross-link entitas → Mapping Hub sub-page. Tegaskan "source of truth ada di Hub".

---

## 🗂 Key Data Contracts

### Mock IDs (jangan ubah tanpa update semua tab)

- **IGD**: `igd-1` (Joko Prasetyo ♂55, `RM-2025-005`) · `igd-2` (Siti Rahayu ♀32, `RM-2025-012`)
- **RI**: `ri-1` (GJK NYHA III, dr. Budi Santoso Sp.JP, `RM-2025-003`) · `ri-3` (Syok Sepsis, dr. Hendra Wijaya Sp.EM, `RM-2025-007`)
- **RJ**: `rj-1` · `rj-2`
- **Mock keyed by `RM-2025-003`**: `KONSULTASI_MOCK` · `OrderLabMock` · `OrderRadMock` · `DISCHARGE_MOCK` · `PASIEN_PULANG_MOCK` · `GIZI_HISTORY_MOCK`
- **Mock keyed by `RM-2025-005`**: `HANDOVER_MOCK` (IGD)
- **Farmasi orders**: 5 lintas unit — `igd-1` (HAM, Depo IGD) · `igd-2` (Depo IGD) · `ri-1` (HAM, Apotek RI) · `ri-3` (Apotek RI) · `rj-1` (Apotek RJ) → `farmasi/farmasiShared.ts`
- **Radiologi orders**: 5 lintas unit — `igd-1` (Foto Thorax AP CITO) · `igd-2` (USG Abdomen Semi-Cito) · `ri-1` (CT Thorax kontras Rutin) · `ri-3` (Foto BNO 3 posisi) · `rj-1` (USG Tiroid) → `rad/radShared.ts`

### Core Types (semua di `src/lib/data.ts`)

- `IGDPatientDetail` · `PatientMaster` · `KunjunganRecord` · `RawatInapPatientDetail` · `RJPatientDetail`
- `TipePenjamin`: `BPJS_Non_PBI | BPJS_PBI | Umum | Asuransi | Jamkesda`
- `RIKelas`: `VIP | Kelas_1 | Kelas_2 | Kelas_3 | ICU | HCU | Isolasi`
- `DiagnosaTipe`: `Utama | Sekunder | Komplikasi | Komorbid`
- `DiagnosaStatus`: `Pasti | Dicurigai | Diferensial`
- `IGDDiagnosa`: `id · kodeIcd10 · namaDiagnosis · tipe · status? · alasan? · analisa?`
- `CPPTEntry`: `id · waktu · tanggal? · profesi · penulis · SOAP fields · verified? · verifiedBy? · verifiedAt? · flagged?`

### Shared Medical Records (`src/components/shared/medical-records/`)

| Component | Used By | Notes |
|---|---|---|
| `TTVTab` | IGD · RI · RJ | `triage?` IGD obs mode · `history?` RI multi-shift · GCS auto-calc + NEWS2 score |
| `CPPTTab` | IGD · RI · RJ | `showDate` RI · `requiresVerification` DPJP co-sign · **DB-wired** (UUID `kunjunganId` → persist per-aksi `medicalrecord.Cppt`, jenis SOAP/SBAR/TBAK, gate `clinical.cppt`) · **default jenis = SBAR** (prop `defaultJenis`, 2026-07-01) |
| `DiagnosaTab` | IGD · RI · RJ | ICD-10 + ICD-9 + status + INA-CBG preview |
| `TindakanTab` | IGD · RI · RJ | **Shared ✅ (2026-07-13)** — search-first katalog ter-assign (`/master/tindakan-tersedia`) + kartu konfigurasi + estimasi biaya. Adapter tipis per unit set `jenisRuangan` (tier tarif: IGD · `RAWAT_INAP:<kelas>` · RAWAT_JALAN) + `unitLabel`. **DB-wired** kunjunganId UUID → `medicalrecord.TindakanMedis` via `/kunjungan/:id/tindakan` (gate `clinical.tindakan`, snapshot harga/penjamin/jenisRuangan). RJ tarif belum di-seed → "Belum bertarif" (graceful) |
| `HandoverTab` | IGD · RI | SBAR 4-seksi + auto-populate TTV |
| `DaftarOrderTab` | IGD · RI · RJ | **DB-driven ✅ (2026-06-22):** kunjungan UUID → gabung **Resep (`listResep`) + Lab (`listLabOrders`)** lintas-jenis (`mergeDbOrders`, urut terbaru), detail item + **Timeline Status** vertikal per-jenis ([OrderTimeline](src/components/shared/medical-records/daftarOrder/OrderTimeline.tsx): tahapan native Resep/Lab + waktu order; Dibatalkan/Ditolak = kartu terminal); Batalkan → API nyata (`cancelResep`/`cancelLabOrder`, hanya saat `nativeStatus="Menunggu"`). Latar kartu order **disesuaikan status** (`STATUS_CARD` tint). **Widget Estimasi Biaya** ([OrderCostSummary](src/components/shared/medical-records/daftarOrder/OrderCostSummary.tsx)): akumulasi tarif per jenis (Resep/Lab/Rad/BMHP) + grand total, Dibatalkan tak dihitung (`costByType`/`orderCost`); tarif = snapshot `item.harga` (Lab dari Tarif Matrix · **Resep dari `master.Obat.hargaSatuan` × jumlah ✅ 2026-06-22** · Rad/BMHP belum ada order DB → 0; per-item harga tampil di detail kartu). Pasien demo (non-UUID) → `ORDERS_MOCK`. `kunjunganId=patient.id` dari adapter IGD/RI/RJ |
| `OrderLabTab` · `OrderRadTab` | IGD · RI · RJ | Mirror dari worklist lab/rad. **OrderLab DB-driven ✅ (2026-06-21):** katalog = tes TER-ASSIGN ke ruangan Laboratorium (`GET /master/lab-test-tersedia`, gate `clinical.tindakan:read`, distinct + harga dari Tarif Matrix per tier unit pengirim); Paket Cepat di-resolve dari master by kode + total harga; Panduan dihapus; estimasi biaya live. File di-split → `shared/medical-records/orderLab/{orderLabShared,orderLabMock,OrderLabHistory}`. **Order persist ✅ (2026-06-21):** submit saat kunjunganId UUID → `medicalrecord.LabOrder` via `POST /kunjungan/:id/lab` (gate `clinical.tindakan`); item snapshot harga; worklist Lab endpoint siap (`GET /lab/orders`). Pasien demo (non-UUID) tetap lokal. **Riwayat Order Lab ✅ (2026-06-21):** panel [RiwayatOrderLab](src/components/shared/medical-records/orderLab/RiwayatOrderLab.tsx) (mirror RiwayatOrderResep) — pasca-kirim order (UUID) form dibersihkan + panel refetch (tanpa takeover layar sukses); fetch `listLabOrders` + filter bucket + expand item + CITO + **Salin** (re-order) + **Batalkan** (`cancelLabOrder`, gate `clinical.tindakan:update`); helper `labOrderBucket/labOrderStatusCfg/labOrderRowBg` di orderLabShared. **Lihat Hasil ✅ (2026-06-22):** order **Selesai** → tombol **"Lihat Hasil"** (chip emerald di baris) buka **modal** `LabHasilModal` (header gradient sky→indigo + meta strip CITO/Kritis/Abnormal/total, body hasil dikelompokkan kategori + nilai/satuan/rujukan/flag warna, footer analis/validator/rilis + catatan; Escape/klik-luar tutup, animasi) — parameter terisi saja; via endpoint klinis baru `GET /kunjungan/:id/lab/:labId/hasil` (gate **`clinical.tindakan:read`** + ABAC careUnit; bukan ancillary worklist — klinisi tak punya itu); service `labResultService.getHasilForKunjungan` (verifikasi order∈kunjungan anti-IDOR), client `getLabResultForKunjungan`; status proses → hint "sedang diproses". **OrderRad DB-driven ✅ (2026-06-22):** katalog = pemeriksaan TER-ASSIGN ke ruangan **Radiologi** (`GET /master/rad-catalog-tersedia`, gate `clinical.tindakan:read`, distinct + harga dari Tarif Matrix per tier unit pengirim; layered DAL `listAssignedRadCatalog`→Service `listRadCatalogTersedia` format TAT/persiapan dari JSONB→route `scopeKunjungan:false`); **Panduan Order Radiologi dihapus**; Paket Cepat di-resolve dari katalog by kode DB (`RAD-NNNN`) + total harga (paket tanpa anggota disembunyikan); harga per item + Est. total footer; loading/error state. **`RAD_CATALOG_SEED` tak lagi dipakai di tab ini** (masih dipakai Beranda). **Kontrak order rad ✅ (2026-06-22):** `medicalrecord.RadOrder`+`RadOrderItem`+`RadResult`+`RadResultItem` (migrasi `20260622130000`, mirror Lab; hasil rad = ekspertise narasi temuan/kesan per pemeriksaan + criticalNotifs ACR + validasi radiolog) · Zod/DTO `schemas/rad/{radOrder,radResult}` · API client `api/rad/{radOrder,radResult}` · helper SDM Assignment `services/rad/radAssignment.ts`. **Order→Terima LIVE ✅ (2026-06-22):** DAL `radOrderDal` + Service `radOrderService` (create status awal IGD/RI=Menunggu·RJ=Diterima · listForRad · receive **enforce SDM Assignment** `assertActorAssignedToRad` · cancel · listPetugas) + route `POST/GET /kunjungan/:id/rad` + `/:radId/cancel` (gate `clinical.tindakan`) + `GET /rad/orders` · `/:id` · `POST /:id/receive` · `GET /:id/petugas` (gate `ancillary.rad.worklist`, RBAC sudah seed Radiografer/SpRad). **FE:** OrderRadTab submit saat kunjunganId UUID → `createRadOrder` (tanpa takeover: form dibersihkan + panel riwayat refetch); **Riwayat Order Rad** ✅ [RiwayatOrderRad](src/components/shared/medical-records/orderRad/RiwayatOrderRad.tsx) (mirror RiwayatOrderLab) — fetch `listRadOrders`, filter bucket (Belum Diterima/Diproses/Selesai), expand item (modalitas/TAT/tarif), CITO, **Salin** (re-order) + **Batalkan** (`cancelRadOrder`, gate `clinical.tindakan:update`); demo (non-UUID) → mock RiwayatRadSection. **Rad worklist** [RadInbox](src/components/rad/RadInbox.tsx) (DB) di [RadPageView](src/components/rad/RadPageView.tsx) — "Order Masuk dari Klinis" + **Terima** (`receiveRadOrder`) + badge pending. **Worklist Rad MOCK DIHAPUS ✅ (2026-06-22)** — `RadBoard` (mock `deriveRadOrders`) tak lagi dirender di worklist + header stat mock di `(main)/radiologi/page.tsx` dibuang. **Worklist card-grid + Detail Order DB-driven (desain dipertahankan) ✅ (2026-06-23):** RadInbox order = **grid kartu responsif** (`sm:2 / xl:3` kolom) "Belum Diterima" + "Dalam Pengerjaan" (klik → detail). **Detail order = desain mock asli dipertahankan** (`RadOrderHeader` + `RadOrderTabs` 8 pane Verifikasi/Persiapan/Akuisisi/Expertise/Validasi/Viewer/Kontras/Riwayat) tapi **data dari DB**: [RadOrderWorkspace](src/components/rad/RadOrderWorkspace.tsx) fetch `getRadOrder`+`getRadResult` → `mapToRadOrder` (status/modalitas/prioritas DB→vokabuler desain) → render; `RadOrderTabs` jadi **controlled** (`order`+`onRefresh`). **Ekspertise + Validasi PERSIST ✅** — `EkspertasiPane` (Simpan Draft / Terbitkan) → `saveRadResult` (`finalize` flag); `ValidasiPane` → `validateRadResult`; **kontrak RadResult disesuaikan ke desain** = laporan tunggal (indikasiKlinis/teknik/temuan/kesan/saran + radiolog/radiologSip + criticalFindings JSONB) via kolom additif (migrasi `20260623120000`, `rad_result_item` ditinggalkan). BE: `radResultDal`/`radResultService` (`saveHasil` finalize→Diterima/Diperiksa→Divalidasi · `validate`→Selesai, atomik; SDM Assignment `assertActorAssignedToRad` aktor; validator nama free-text sesuai desain) + routes `GET/POST /rad/orders/:id/hasil` · `POST /:id/validasi` (gate **`ancillary.rad.expertise`**) · `GET /kunjungan/:id/rad/:radId/hasil` (klinis `clinical.tindakan:read`). Billing ingest (client) tetap saat validasi. **Verifikasi Identitas (SKP 1) selaras Lab ✅ (2026-06-23):** [VerifikasiPane](src/components/rad/tabs/VerifikasiPane.tsx) — "Diterima Oleh" = **user login** (read-only, badge, bukan free-text lagi) + **SDM Assignment guard** (`useRadRoster`, banner "Belum Ditugaskan ke Radiologi") + layout detail di-wrap `SessionProvider`; pola identik Penerimaan Lab (penerima sesi + receive di worklist). **Akuisisi & Dosis PERSIST ✅ (2026-06-23):** tab Akuisisi (OPSIONAL) → kontrak baru `medicalrecord.RadAkuisisi` (append-only, migrasi `20260623140000`; `radiografer`/`paramTeknis`/`proteksi`/`dosis` JSONB variatif per modalitas, dosis & proteksi NULL utk non-pengion USG/MRI) · layered `schemas/dal/service/api rad/radAkuisisi` + route `GET/POST /rad/orders/:id/akuisisi` (gate `ancillary.rad.worklist`, SDM Assignment `saveAkuisisi`, transisi opsional Diterima→Diperiksa); form: waktu=DateTimePicker · **radiografer multi-select** [RadiograferPicker](src/components/rad/RadiograferPicker.tsx) dari roster (default user login) · sadar `isIonizing` (dosis/proteksi/DRL hanya pengion); wired di RadOrderWorkspace (`getRadAkuisisi`→`order.akuisisi`). **Ekspertise & Validasi DIGABUNG + Cetak TTE ✅ (2026-06-23):** tab "Validasi & Rilis" dihapus dari nav (file ValidasiPane disisakan); `radResultService.saveHasil(finalize)` kini = terbit+validasi+rilis sekaligus → order langsung **Selesai** (validator = radiolog penulis = penanda tangan, validatedAt di-stamp saat create; tanpa migrasi); EkspertasiPane (jadi "Expertise & Validasi") + billing ingest saat terbit + tombol **Cetak Hasil** → [RadPrintModal](src/components/rad/RadPrintModal.tsx) A4 narasi + **QR TTE** radiolog (`TteQr` dipindah ke [shared/TteQr](src/components/shared/TteQr.tsx), serial `TTE-RAD-…`, payload `EHIS-RAD|…`); header timeline 4→**3 step** (Verifikasi→Akuisisi→Expertise & Validasi). **Sisa:** klinis "Lihat Hasil" di RiwayatOrderRad · pane Persiapan/Kontras/Viewer masih lokal (tanpa DB backing). |
| `InformedConsentTab` | IGD · RI · RJ | Template per tindakan + TTD digital. PMK 290/2008 |
| `RekonsiliasTab` | IGD · RI | `context:"igd"\|"ri"` → phase labels berbeda. HAM badge + progress bar |
| `KonsultasiTab` | RI · RJ | SBAR closed-loop + 22 SMF dropdown |
| `SuratDokumenTab` | RJ | 4 jenis surat. PMK 269/2008 |
| `ResepTab` | IGD · RI · RJ | `showMAR` flag · HAM badge + HAMConfirmModal · decision-support (2026-06-18): Select global · Dosis sekali minum · peringatan alergi obat sadar-golongan · 3 dropdown kondisi klinis · no. kontak DPJP. **BE order ✅ (2026-06-19):** `medicalrecord.ResepOrder`+`ResepItem` (tabel sendiri, BUKAN domain Order generik) · `POST/GET /kunjungan/:id/resep` (gate `clinical.resep`) · worklist `GET /farmasi/resep` (gate `ancillary.farmasi.telaah`) · depo = dropdown lokasi Farmasi master (`GET /master/lokasi-farmasi`) · cari obat = `GET /master/obat-tersedia` (ter-formularium, fallback mock) · submit saat kunjunganId UUID → muncul di **Worklist Farmasi** (`mapDbResepOrder`). **Harga snapshot ✅ (2026-06-22):** `ResepItem.harga Int?` (migrasi `20260622120000`) = `master.Obat.hargaSatuan` saat order; `obat-tersedia` kini expose `hargaSatuan` → form bawa harga → biaya baris = harga × jumlah (dipakai widget Estimasi Biaya Daftar Order). **TTE + Cetak ✅ (2026-06-19, IGD):** hanya dokter yang order (gate `clinical.resep:create`) · resep auto-TTE saat create (barcode + serial, mock always-success) · preview & cetak A4 modern (`shared/resep/{TteBarcode,ResepCetakTemplate,ResepCetakModal}`). Panduan Aturan Resep dihapus. **Catatan: belum benar-benar 1 komponen** — IGD pakai `igd/tabs/ResepPasienTab`, RI/RJ pakai `shared/.../ResepPane` (TTE+cetak baru di IGD); logika dibagi via `shared/resep/{resepShared,ResepKlinisPanel}`. **Riwayat Order Resep di RI/RJ ✅ (2026-07-13):** [ResepPane](src/components/shared/medical-records/resep/ResepPane.tsx) kini render [RiwayatOrderResep](src/components/shared/resep/RiwayatOrderResep.tsx) (pola RiwayatOrderBmhp) — daftar order DB `GET /kunjungan/:id/resep` + status pemenuhan Farmasi, **latar per-status** (`resepOrderRowBg`), **Salin** (`onCopy`→draft re-order) + **Batalkan** (`cancelResep`, gate `clinical.resep:update`, hanya status Menunggu), filter bucket, expand detail; `refreshKey` (BARU di RiwayatOrderResep) di-bump pasca-kirim → auto-refetch. Muncul utk kunjungan UUID (demo → tersembunyi, RiwayatSection lokal tetap utk template copy) |
| `FarmasiTab` | IGD · RI · RJ | Per-patient status tracker dari `workflowStore` |
| `MARTab` | RI | Medication Administration Record per shift. SNARS PKPO 6 |
| `KonselingTab` | RI | Discharge counseling. SNARS PP 5 |
| `IdentitasVerifikasiBanner` | IGD · RI | Lazy intercept tab aksi (SKP 1 · JCI IPSG 1) |
| `StatusFisikPane` | IGD · RI · RJ | 11-sistem head-to-toe |

Shared asesmen: `src/components/shared/asesmen/` → `AllergyPane` · `RiwayatPane` · `GiziPane` · `asesmenShared.ts`.

**Header widget — Total Tagihan ✅ (2026-07-13 · re-source proyeksi 2026-07-19):** [TotalTagihanWidget](src/components/shared/medical-records/TotalTagihanWidget.tsx) di header rekam medis IGD/RI/RJ (breadcrumb `ml-auto`) = chip akumulasi biaya kunjungan + popover rincian per-kategori. **Kini SELARAS dengan tagihan nyata** — pasien UUID baca **proyeksi billing** via `getBillingRingkas` (`/kunjungan/:id/billing/ringkas`, `byKategori`) → total = `subtotal` proyeksi (identik `BillingMiniWidget`), rincian per kategori proyeksi **Akomodasi + Tindakan + Lab + Rad + Obat & BMHP + Jasa Dokter + Lain-lain** (Akomodasi RI kini IKUT; sebelumnya cuma 5 order klinis tanpa akomodasi, 1 fetch bukan 5). Beda dari `BillingMiniWidget` (SISA = grandTotal − pembayaran) — ini TOTAL/gross (sebelum diskon/bayar). Reaktif via **`recordBus "order"`** (emit dari API client saat create/cancel/update order). Demo (non-UUID) → `ORDERS_MOCK` (Resep+BMHP→"Obat & BMHP"; tanpa akomodasi).

---

## 🚦 Workflow

- **Saat menyelesaikan task**: (1) centang di file aktif (CLAUDE.md atau TODO.md), (2) pindahkan deskripsi detail ke [.claude/DONE.md](.claude/DONE.md), (3) catat tech debt baru di [TECH_DEBT.md](TECH_DEBT.md).
- **Saat menemukan gap klinis**: catat di [.claude/GAP_ANALYSIS.md](.claude/GAP_ANALYSIS.md) sebelum mulai implementasi.
- **Saat mulai modul baru**: cek [TODOS_BACKEND.md](TODOS_BACKEND.md) untuk lihat dependensi backend yang perlu diketahui.
- **Sebelum commit**: jalankan `npx tsc --noEmit` untuk verifikasi types clean.
- **Komitmen file size**: tidak ada file >800 line. Jika lewat → split jadi sub-components (lihat pola di `farmasi/`, `lab/`, `rad/`, `master/mapping/`).
